# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/beads_viewer — `bv --robot-triage` and the robot path
# Total Passes: 10 (minimum)
# Started: 2026-06-07

## Status: IN PROGRESS — Pass 2 of 10
## HONEST baselines (warm, ISOLATED cache via XDG_CACHE_HOME, this repo)
The originally-recorded 2.87/0.90/8.40 were contaminated: insights 8.4s was a one-time
COLD exact-betweenness compute; plan 0.90s reflected a 6.6MB GLOBAL cache holding other
projects' entries. True steady state with a healthy per-repo cache:
- triage:   2.295 s   (≈2.1s of this is the correlation git fan-out; plan path is 0.18s)
- plan:     0.176 s
- insights: 0.819 s   (warm/cached; COLD exact compute is ~8.4s — a separate target)
- cold cache-bloat case (6.6MB global cache) remains real-world relevant for codec/shape passes.
## REORDERED missions (front-load biggest levers; breakthroughs first)
2. Correlation-BatchGit (THE triage breakthrough): 336 `git show` -> 1 `git log --name-status --numstat`.
3. Insights-Betweenness: cold 8.4s exact -> approx/parallel/iterative (franken_networkx CSR/Brandes).
4. DiskCache-FastCodec: stdlib encoding/json -> goccy; streaming read (helps cold + bloated cache + insights write).
5. DiskCache-CompactShape: shrink payload (int-indexed arrays vs per-node map[string]float64).
6. Insights-CyclesHitsEig: iterative-convergence + caps; skip-when-dense.
7. XFetch/Correlation-Scope: stop near-constant XFetch recompute on tiny/fast repos; bound correlation.
8. Loader-Allocs: cut 22.8k allocs / 7.7MB on the 1.9MB JSONL parse.
9. DataHash: faster/incremental hashing (avoid full sort+SHA256 each call).
10. GraphBuild-IntIndexed + final: CSR adjacency + int-indexed metric write-back; end-to-end re-bench.
## Missions (ordered by Impact×Confidence/Effort)
1. DiskCache-NoRewriteOnHit: stop rewriting the 6.6MB analysis_cache.json on every read/hit (LRU touch). Affects ALL robot cmds.
2. DiskCache-FastCodec: replace stdlib encoding/json with goccy/go-json for cache (de)serialize; streaming read.
3. DiskCache-CompactShape: shrink cache payload — int-indexed arrays instead of per-node map[string]float64; cap/omit redundant maps.
4. Correlation-BatchGit: replace 336 `git show` (2/commit) with ONE `git log --name-status --numstat <range>`; reuse snapshot stream parser.
5. Correlation-Scope: bound/cache/parallelize remaining correlation work for triage; ensure --robot-next doesn't over-correlate.
6. Insights-Betweenness: size-gate to approximate sooner + parallelize/iterative Brandes (mine franken_networkx CSR/iterative).
7. Insights-CyclesHitsEig: iterative-convergence + caps for cycles/HITS/eigenvector; skip-when-dense; franken_networkx techniques.
8. Loader-Allocs: cut 22.8k allocs / 7.7MB on the 1.9MB JSONL parse (goccy decoder reuse, buffer pooling).
9. DataHash: faster + incremental hashing (avoid full sort+SHA256 each call); xxhash/structural.
10. GraphBuild-IntIndexed + final: CSR adjacency + int-indexed metric write-back (alien-graveyard/alien-artifact ceiling); end-to-end re-bench + artifact update.
## Completed Passes
(none yet)
### Pass 1 — DiskCache-NoRewriteOnHit — b5ed373
- Files: pkg/analysis/cache.go (+ cache_test.go benchmark)
- Change: getRobotDiskCachedStats no longer rewrites the whole cache file on a read-hit.
- Bench (warm isolated): insights 819->663ms (-19%); triage 2.295->2.259s; plan flat.
- Verdict: PRODUCTIVE (correct; biggest effect on large cached payloads & bloated caches).
### Pass 2 — Correlation-BatchGit — 974ca5e
- Files: pkg/correlation/cocommit.go
- Change: 336 per-commit `git show` -> 2 batched `git log --no-walk` (name-status+numstat), memoized.
- Bench: git execs 340->6; triage 2.30s->1.21s (1.87x); next 2.25s->1.21s. PRODUCTIVE.
### Pass 3 — Correlation-LifecycleExtraction — d76cd8c
- Files: pkg/correlation/extractor.go, extractor_snapshot.go (+ harness/goldens)
- Change: dropped `git log -p` over issues.jsonl; --raw blob-OID snapshots, deduped cat-file, maphash multiset; gate 48MB->64KB.
- Bench: triage 1.21s->1.025s; next ->1.016s. Differential 1776==1776 events. PRODUCTIVE.
## Status: IN PROGRESS — Pass 4 of 10 (Correlation result cache)
### Pass 4 — Correlation-ResultCache — 936dbca
- Files: pkg/correlation/disk_cache.go (new), file_lock_unix/windows.go (new), cmd/bv/robot_registry.go
- Change: persistent HistoryReport cache keyed HEAD+beads-hash+opts+version (goccy); skips git extraction on hit.
- Bench: repeat triage 1.02s->152ms (6.7x); cold parity; invalidation verified. PRODUCTIVE.
### Pass 5 — Datasource-Validate-Dedup — (this commit)
- Files: internal/datasource/validate.go (+ goldens)
- Change: typed probe instead of map[string]interface{}; path/mtime/size validation cache (validate once not 2-3x).
- Bench: warm triage 152->108ms (-27%); next ->106ms. base==cand identical. PRODUCTIVE.
## Status: IN PROGRESS — Pass 6 of 10 (insights cold: exact betweenness)
## Note (post-pass-5 re-measurement)
- The originally-reported "insights 8.4s" was a CONTAMINATED-CACHE ARTIFACT, not a real hotspot.
  Insights cold is now 0.51-0.75s with ALL metrics computed (approx betweenness sample=100, ~1.6ms;
  every metric sub-2ms). Dropped the "insights betweenness/cycles breakthrough" missions as phantom.
- Real remaining levers (perf-attributed on warm plan/triage floor ~110ms):
  (a) analysis disk cache READ still uses stdlib encoding/json (~6%; ~0.9s on a bloated 6.6MB cross-project cache).
  (b) JSONL parsed TWICE (datasource validate probe + loader parse) over the same 1.9MB.
  (c) cold triage 1.05s = correlation extraction (232MB blob I/O) on first call after a change.
  (d) Go binary startup/init overhead (~70ms of the 110ms; heavy deps).
### Pass 6 — AnalysisDiskCache-FastCodec — 7a457b8
- File: pkg/analysis/cache.go. stdlib encoding/json -> goccy streaming+compact.
- Bench: bloated 13MB cache read 2.29x (325->142ms); small plan 110->98ms, triage 123->113ms. PRODUCTIVE.
### Pass 7 — AnalysisCache-CompactShape — (this commit)
- Files: pkg/analysis/cache.go (+ cache_extra_test.go, cache_test.go). Columnar SoA blob, version 1->2.
- Bench: cache -44%/-58%, decode 1.61x; insights 489->377ms, triage ->116ms. ubs 0/0. PRODUCTIVE.
## Status: IN PROGRESS — Pass 8 of 10 (parse JSONL once: loader/validate dedup)
### Pass 8 — Datasource-ParseOnce — 11bc515
- Files: internal/datasource/load.go, select.go, pkg/loader/loader.go, cmd/bv/main.go
- Change: loader's tolerant parse doubles as validation (ParseStats hook); robot path parses issues.jsonl once.
- Bench: opens 2->1; plan 108->95ms, triage 121->114ms. Full suite+e2e green. PRODUCTIVE.
## Status: IN PROGRESS — Pass 9 of 10
### Pass 9 — ComputeDataHash-Dedup — (committed)
- Files: pkg/analysis/{graph,cache,triage}.go, cmd/bv/{main,robot_registry}.go
- Change: memoize data hash (sync.Once) + thread once through dispatch; data_hash unchanged.
- Bench: hash calls 2->1; plan 89->85ms, triage 105->102ms, insights 395->368ms. PRODUCTIVE.
## Status: IN PROGRESS — Pass 10 of 10 (parallel JSONL load + final wrap)
### Pass 10 — Loader-ParallelParse — ff1bc91
- Files: pkg/loader/loader.go (+ parallel_diff_test.go, benchmark_test.go, HOTSPOT_TABLE.md)
- Change: size-gated (>=4MiB) morsel-driven parallel JSONL parse; serial below crossover (GC-bound). -race + differential proven.
- Bench: net-neutral on 1.9MB repo (no regression), +10-21% on multi-MB stores. PRODUCTIVE (future-proof).
## Status: COMPLETE — 10/10 passes done. Full suite green (e2e incl). 0 failures.
## FINAL (orig 0ef0e25 vs final, isolated cache, median-of-3):
##   triage  warm 2.24s -> 0.09s (~25x), cold 2.20s -> 0.98s
##   next    warm 2.22s -> 0.08s (~28x), cold 2.20s -> 0.98s
##   plan    warm 0.14s -> 0.06s (~2.3x)
##   insights warm 0.63s -> 0.19s (~3.3x); git execs(triage) 340 -> 3
## EXTENSION — cold first-call (user-approved: safe wins + incremental extraction)
Cold profile (0.98s): correlation extractViaSnapshots ~31% (readBlobs/readFull 17%, parseDiff/parseBeadJSON 6% stdlib-json) + git inflate/pipe of 232MB (~200 historical issues.jsonl snapshots).
11. Correlation-CacheSplit: extraction is HEAD-only but report cache keys on beads-hash too -> br-edit busts the 232MB extraction. Memoize []BeadEvent keyed on HEAD(+opts) alone; reassemble report per beads-hash cheaply.
12. Correlation-ParseBeadGoccy: parseBeadJSON stdlib encoding/json -> goccy + drop checkValid.
13. Correlation-IncrementalExtract: content-addressed per-commit/blob-OID-pair memo so HEAD-advance only processes NEW commits (alien-graveyard content-addressed memoization).
## Status: IN PROGRESS — Pass 11 (Correlation-CacheSplit)
### Pass 11 — Correlation-CacheSplit — d02f5cb  | edit->triage 0.98s->0.15s
### Pass 12 — Correlation-ParseBeadGoccy — f2383c5 | parseBeadJSON goccy
### Pass 13 — Correlation-IncrementalExtract — 609912f | new-commit->triage 0.99s->0.40s; 0 blobs when warm
## Status: COMPLETE — 13 passes. Cold-path scenario matrix (orig 0ef0e25 -> final):
##   warm-repeat 2.26s->0.10s | edit->triage ->0.15s | new-commit->triage ->0.40s | cold-first-ever ->1.01s
## Next candidate (not done): co-commit primeBatch -> per-commit SHA cache (residual on new-commit path).
## Pass 14 — Correlation-CoCommitIncremental (TODO tracker)
- [x] P14.1 investigate cocommit.go (primeBatch memoizes fileCache/statCache per SHA; pure fn of SHA+excludePathspecs)
- [x] P14.2 design per-commit co-commit cache (SHA key, namespaced by exclude-pathspec hash, goccy, flock, bounds)
- [x] P14.3 implement per_commit_cocommit_cache.go + wire primeBatch (disk-load known, git only unknown, persist fresh)
- [x] P14.4 differential test (full/cold/warm/k-new byte-identical; fetch count == uncached count)
- [x] P14.5 verify: goldens cold+warm, -race, correlation+cmd/bv tests; handle date-drift goldens (regen if byte-identical to HEAD)
- [x] P14.6 gates: build/vet/gofmt/ubs (confirm ubs criticals are the established benign patterns)
- [x] P14.7 benchmark new-commit path (co-commit git calls 2->0 confirmed; warm/edit/cold unchanged)
- [x] P14.8 commit pass 14 (agent committed 7e8a6bd+5c483de; add golden regen if needed)
- [x] P14.9 final comprehensive re-benchmark (orig vs final all scenarios) + artifact/memory update + cleanup
## Fresh-eyes bug review (4 parallel adversarial review agents over the 19 changed files)
Found + fixed (all output-preserving; candidate byte-identical to pre-fix HEAD on all 4 robot cmds):
- BUG-A1 (SERIOUS): head_artifact_cache dropped CorrelatedCommit.BeadID (json:"-") -> middle-tier
  (edit-bead->triage) produced commit-less reports. Fix: custom historyArtifact (Un)MarshalJSON
  preserving commit BeadIDs via parallel array. Regression tests added. E2E: total_commits 275==275.
- BUG-D1 (SERIOUS): loadSmart accepted a fresher non-issue/empty-content JSONL (sprints.jsonl) as
  0-issue "success", shadowing real issues.jsonl. Fix: ParseStats.Skipped counter + reject candidate
  with 0 valid issues from non-empty content; empty-project still valid. Regression tests added.
- (caught by pass-10 differential test) parallel parser didn't merge ParseStats.Skipped -> fixed merge.
- BUG-B1: transient git failure in primeBatch persisted empty co-commit entries (poison 30d). Fix:
  batchFilesChanged/batchLineStats return error; skip disk persist on failure (in-memory degrades for run only).
- RISK-B2: readBlobs could deadlock on a cat-file parse error mid-stream (full pipe). Fix: Kill git on
  parse error before draining writeErr (mirrors legacy -p path).
- BUG-D2: parallel parser used > maxCapacity vs serial's >= (bufio isPrefix) -> exactly-maxCapacity line
  diverged. Fix: >=.
- BUG-D3: defaultProbe treated present-but-null required field as absent (vs old map semantics + rawProbe).
  Fix: non-pointer json.RawMessage values, presence = len>0.
- SMELL-D5: corrected inaccurate json.Decoder comment.
Reviewed-correct (no bug): analysis SoA cache round-trip, version gating, DataHash memoization/seeding,
no-rewrite-on-hit, goccy codec, incremental-extract order composition, blob-OID validation.
Not changed (assessed low/narrow): SMELL-B3 dup-id winner (one-record-per-id invariant), RISK-D4
validation-cache mtime granularity in watch sessions (nanosecond mtime makes it ~unreachable).
---
# 2026-08-23 campaign — 50-pass cap
Skill: `extreme-software-optimization`, orchestrated by
`repeatedly-apply-skill`. This campaign starts from
`4fc261a1a7f3c885a6a06c272a954d8954978a52`; the 14 historical passes above do
not count toward this campaign.
## Status: COMPLETE — converged after pass 8 of at most 50 independently verified
The target is the robot-first product path, with secondary coverage for loader,
analysis, search, TUI graph/snapshot, export layout, watcher reload, and
correlation. Every pass must cite a current profile or a prior pass's measured
residual, preserve robot outputs or the narrower behavior oracle, include a
planted negative check, keep stderr visible, and receive orchestrator re-run at
the resulting revision. Two consecutive evidence-backed zero-change passes end
the loop as converged; 50 is a cap, not a mutation quota.
## Current profile
- Pinned source snapshot: RCH `beads_viewer/2f64b06266de270c`; four key SHA-256
  receipts matched the local checkout.
- Low-load profile host: `hz1`, 1-minute load 1.52-3.77 during measurements,
  Go 1.25.5, Linux/amd64.
- 540 issues / 685 blocking edges: load 23.76 ms, graph build 0.90 ms,
  phase 1 0.98 ms, phase 2 8.02 ms, total profiled startup 32.76 ms.
- Warm-cache `--robot-insights`: 0.21-0.31 s; graph metrics total under 10 ms,
  so remaining time is load/fixed startup, what-if/advanced-insight assembly,
  metric-map materialization/sorting, cache decode, and encoding.
- The Mac's load exceeded 100, so no local latency result is admissible.
- Full triage-history timing remains unprofiled on `hz1` because the RCH source
  mirror intentionally excludes `.git`.
- Negative evidence and retry predicates live in
  `tests/artifacts/perf/HYPOTHESIS_LEDGER.md`.
## Ordered missions
1. ProfileLifecycle-RobotExit: make `--cpu-profile` finalize before registry robot exit; prove non-empty readable profiles and failure behavior.
2. Benchmark-FreshAnalysis: separate fresh analysis from the five-minute incremental-cache hit path.
3. Benchmark-CacheDecode: isolate v3 entry read/decode from 4,000-issue analyzer construction and hashing.
4. Insights-TopWhatIfStatsReuse: eliminate repeated `Analyze()` calls inside per-issue what-if computation.
5. Insights-WhatIfStructureHash: determine whether repeated structural hashing remains after stats reuse and remove only with proof.
6. Insights-TransitiveSimulation: profile per-candidate cascade BFS and seek an equivalent shared-index formulation.
7. Insights-MapLimitTopK: replace full map materialize/sort for bounded insights maps if profiles show it material.
8. Insights-MapCopyElision: avoid defensive full-map copies where the encoder consumes immutable completed stats synchronously.
9. Insights-AdvancedReuse: identify and deduplicate analysis work shared by standard, what-if, and advanced insights.
10. Insights-Encoding: profile compact JSON and TOON output separately; reduce allocations without schema drift.
11. Loader-Discovery: attribute the gap between 7.7-10.3 ms direct JSONL load and 23.76 ms CLI load.
12. Loader-SerialDecode: profile goccy decode, normalization, validation, and deep-copy allocation on the 1.37 MB file.
13. Loader-Preallocation: validate issue/dependency/comment capacity estimates against real corpora before tuning.
14. Loader-ParallelThreshold: remeasure the 4 MiB/512-line crossover on the current Go version and worker class.
15. Loader-ParallelAssembly: reduce chunk/reassembly allocation only if 8-40 MiB profiles support it.
16. Loader-WarningPath: prove malformed/BOM/CRLF/line-limit cases do not pessimize valid-input parsing.
17. DataHash-SortCopy: profile canonical issue copying/sorting and seek a deterministic lower-allocation representation.
18. DataHash-FieldEncoding: reduce formatting and transient allocation while preserving all material fields.
19. GraphBuild-Maps: profile ID maps, blocker counts, and adjacency growth at 540/1k/10k issues.
20. GraphBuild-AdjacencyCapacity: pre-size compact adjacency only when degree evidence supports the memory tradeoff.
21. Phase1-DegreeIteration: inspect Gonum iterator allocation over compact adjacency.
22. Phase1-TopoSort: compare generic Gonum topo sort with a compact-graph-specific equivalent under differential tests.
23. Phase2-PageRank: retain current implementation unless sparse/dense profiles identify a material residual.
24. Phase2-Betweenness: re-evaluate exact/approximate thresholds only on representative 500/2k/dense graphs.
25. Phase2-HITS-Eigenvector: test convergence/allocation improvements without ranking drift.
26. Phase2-Cycles: test timeout and many-SCC behavior, including post-timeout CPU lifetime.
27. Phase2-Cancellation: make timed-out metric work stop only with a causal goroutine/CPU proof.
28. Phase2-KCore-Articulation: profile the combined computation and avoid duplicate graph traversal if present.
29. Phase2-SlackCriticalPath: share topological data when semantics and cycle behavior match.
30. Analysis-CachePolicy: reproduce cache-on versus cache-off across compute/payload sizes and gate only when beneficial.
31. Analysis-CacheDecode: profile SoA JSON decode and `toGraphStats` allocation after the benchmark seam is repaired.
32. Analysis-CacheWrite: measure marshal/fsync/prune separately on cold recompute.
33. Analysis-CacheValidation: verify top-level `.beads` scan remains bounded under journals and transient locks.
34. Analysis-XFetch: prove refresh probability cannot turn fast-cache hits into near-constant recomputation.
35. Correlation-WarmReport: profile a complete low-load Git checkout with identical source/data fingerprints.
36. Correlation-ColdSnapshot: separate Git subprocess, pipe/blob copy, JSON parse, and report assembly.
37. Correlation-NewCommit: validate O(new commits) behavior for both event and co-commit caches.
38. Correlation-CacheCodec: profile persistent cache read/write and size bounds on 200/500-commit limits.
39. Correlation-TimeoutCleanup: prove timed-out history work and Git children terminate promptly.
40. Search-ColdIndex: profile document sync, embedding, binary persistence, and corruption recovery.
41. Search-WarmTopK: profile dense vector scan and heap cost at 1k/10k documents.
42. Search-HybridMetrics: prevent redundant graph/hash work across warm queries if current profiles show it.
43. TUI-NewModel: profile construction to first render without opening a browser or terminal UI.
44. TUI-SnapshotBuild: compare small/medium/large/huge tiers and pooled parsing.
45. TUI-SnapshotSwap: profile incremental versus full list/component rebuild while preserving selection.
46. TUI-GraphRebuild: reduce the measured 1.23-1.33 ms layered-1k rebuild only if larger sizes breach frame targets.
47. TUI-Render: profile wide 1k/5k/10k graph and tree views for allocation/frame regressions.
48. Export-SQLite: split insert/FTS/materialized-view/optimize costs across issue and comment density.
49. Export-GraphLayout: investigate the 1.26-1.51 ms and 1.01 MB/op layered-1k layout at larger scales.
50. Final-Gauntlet: same-window end-to-end matrix, negative-ledger audit, full verification, and convergence verdict.
## Completed passes
### Pass 1 — ProfileLifecycle-RobotExit — `854a8070`
- Profile citation: the pre-fix `--cpu-profile ... --robot-insights` command
  exited successfully and emitted valid JSON, but left an exactly zero-byte
  profile because registry dispatch terminated through `os.Exit` before
  deferred shutdown ran.
- Change: registry-backed robot dispatch receives an idempotent exit finalizer;
  the CPU-profile finalizer stops profiling and closes the profile file before
  exit.
- Planted negative: the new real-subprocess success subtest failed on the
  pre-fix bytes with `CPU profile is empty`; the invalid-destination subtest
  remained green and produced no success JSON.
- Positive proof: independent RCH execution of
  `TestRobotCPUProfileFinalizedBeforeExit` passed both subtests. The delegated
  live check produced valid JSON, a 4,960-byte profile, and a profile readable
  by `go tool pprof` as `Type: cpu`.
- Gates at this working revision: `gofmt -d` and `git diff --check` clean;
  `go vet ./...`, `go build ./...`, and the full `cmd/bv` package test passed
  on RCH workers.
- UBS disposition: the exact-file scan ran and reported the repository's broad
  heuristic backlog, including pre-existing findings outside the changed
  lines; it is not represented as a zero-finding result.
- Result: PRODUCTIVE measurement-capability repair. No runtime speedup is
  claimed, and direct legacy `os.Exit` branches remain outside this pass.
### Pass 2 — Benchmark-FreshAnalysis — `e4a1d7ac`
- Profile citation: the existing real-data analysis benchmarks returned
  0.565-0.742 ms/op because iterations after the first reused the five-minute
  process-wide graph-stats cache.
- Change: `AnalysisConfig.DisableCache` now bypasses the incremental, robot
  disk, and `CachedAnalyzer` caches without appearing in robot serialization.
  The real-data and synthetic analysis benchmarks opt into fresh computation;
  graph construction stays outside their timers because it has a dedicated
  benchmark.
- Planted negative: a cached result's degree map was mutated to 99. Default
  analysis observed the mutation through the expected cache hit, while two
  disable-cache calls returned distinct stats and the correct degree of 1;
  the outer `CachedAnalyzer` path received the same check.
- Positive proof: independent focused and full `pkg/analysis` tests passed.
  On low-load `hz2` (1-minute load 0.74), the repaired real-data benchmark
  measured full analysis at 36.63-39.96 ms/op, about 2.17 MB/op and 15,267
  allocs/op; fresh phase 1 measured 1.104-1.229 ms/op, 422 KB/op and 2,043
  allocs/op. These replace the contaminated sub-millisecond interpretation.
- Gates: independent `go build ./...`, `go vet ./...`, full
  `pkg/analysis` tests, formatting, and whitespace checks passed. A delegated
  full-repository test attempt ended with RCH exit 143 and no test output, so
  it receives no green credit.
- Result: PRODUCTIVE measurement-semantics repair. No runtime speedup is
  claimed.
### Pass 3 — Benchmark-CacheDecode — `19889a77`
- Profile citation: the prior cache read-hit benchmark included construction
  and hashing of a 4,000-issue analyzer inside every timed iteration.
- Change: the read-side analyzer, seeded data hash, and canceled context are
  prepared before `ResetTimer`; timed iterations still perform the real entry
  read, JSON decode, and `GraphStats` reconstruction, with score and graph-size
  checks on every result. The canceled context prevents XFetch recomputation.
- Planted negative: same-worker `hz2` pre-fix runs measured 40.94-43.29 ms/op,
  16.35-16.37 MB/op, and about 92,530 allocs/op.
- Positive proof: post-fix `hz2` runs measured 25.89-29.71 ms/op,
  11.48-11.57 MB/op, and about 56,700 allocs/op. Independent cache tests and
  a three-iteration benchmark passed; the independent run reproduced about
  56,670 allocs/op.
- Gates: delegated build, vet, cache tests, formatting, whitespace, and exact
  file UBS passed; independent cache tests passed.
- Result: PRODUCTIVE measurement-isolation repair. No product code changed and
  no runtime speedup is claimed. The isolated decoder remains expensive.
### Pass 4 — Insights-TopWhatIfStatsReuse — `4b685960`
- Profile citation: the low-load warm-cache CPU profile attributed about 70 ms
  cumulative to `TopWhatIfDeltas -> computeWhatIfDelta -> Analyze`, while a
  planted 540-issue batch exposed 540 repeated cache decodes.
- Change: `TopWhatIfDeltasFromStats` consumes caller-owned completed stats;
  ordinary `TopWhatIfDeltas` analyzes once per batch, and the registry
  robot-insights handler passes the stats it already owns.
- Planted negative: one low-load pre-fix benchmark iteration took 4.4108 s,
  allocated 1.648 GB, and performed 8,530,586 allocations.
- Positive proof: five same-worker post-fix iterations took 3.203-16.564 ms,
  allocated 1.444-4.931 MB, and performed 8,504-26,341 allocations. Even the
  slowest post-fix iteration was about 266x faster in this microbenchmark.
  A post-fix CPU profile contained neither TopWhat nor its disk-cache decode.
- Behavior proof: normalized robot-insights output at exact pre-pass
  `19889a77` and the candidate had identical SHA-256
  `e5cd6e5bc4dd40c48b5b60c096f0976758d2b8c4d614bae8e45486fa1dd81037`;
  only `generated_at` and status timing fields were removed. A sentinel
  critical-path map proves caller stats are consumed, alongside order/count/
  cap/nil-fallback assertions.
- Gates: delegated low-load package tests/build/vet and independent current-
  tree focused/package tests/build/vet, formatting, and whitespace checks
  passed. UBS's exact-file scan retained one pre-existing timeout heuristic
  outside changed lines, so it is not called clean.
- Rejected intermediate: an incorrect flat remote copy reran old nested source
  and then caused a mixed-package vet failure. No files were deleted; correct
  relative sync in a new preserved tree produced the credited evidence.
- Result: PRODUCTIVE runtime optimization. No claim is made for advanced
  insights, cascade traversal, structure hashing, or the cache codec.
### Pass 5 — Insights-WhatIfStructureHash — zero change
- Source proof: registry robot mode sets `BV_ROBOT=1`; analysis therefore uses
  the memoized data hash and robot disk cache. `graphStructureHash` is confined
  to the mutually exclusive non-robot incremental-cache branch. TopWhat now
  receives handler-owned stats and performs no second analysis.
- Live proof: at fresh 1-minute load 5.75, an exact-source registry-insights
  profile collected 50 ms of samples and contained zero structural-hash
  samples. Sampled work instead included phase 2, load, and robot-cache work.
- Counter-evidence bounded: a non-robot benchmark did attribute structural
  hashing, but it constructs a fresh analyzer every operation and does not
  represent registry insights; per-analyzer memoization cannot improve it.
- Result: ZERO CHANGE (convergence count 1). Retry only if registry mode stops
  selecting robot cache, another analysis enters the handler path, or an
  admissible target profile attributes at least 5% CPU or 1 ms/invocation to
  structural hashing.
### Pass 6 — Insights-AdvancedReuse — `5b73a6b6`
- Reprioritization: pulled forward from mission 9 because the post-pass-4
  profile attributed the remaining redundant analysis/cache read to advanced
  cycle-break insights; the pass-5 structure-hash hypothesis was rejected.
- Change: `GenerateAdvancedInsightsFromStats` and its cycle-break helper consume
  completed caller stats; the ordinary API preserves nil-fallback behavior,
  and registry insights passes its existing stats.
- Planted negative: the post-pass-4 profile retained about 10 ms in
  `GenerateAdvancedInsights -> AnalyzeAsync` solely to read cycles already
  present in handler-owned stats.
- Behavior proof: synthetic supplied cycles control the chosen edge, impact,
  cap and ordering; full serialized output and nil fallback match the ordinary
  API. Exact pre-pass `4b685960` and candidate normalized robot outputs were
  both 69,402 raw bytes and shared SHA-256
  `9e26ba7db25d243e2b5993ef991fd5d14a6e7430946f8f55087e64f99e0df66a`.
- Live proof: at fresh 1-minute load 4.65, the warmed candidate command took
  0.24 s wall, 0.05 s user, 0.03 s system and 40,184 KB RSS. Its readable CPU
  profile contained no advanced-insights, `AnalyzeAsync`, or cache-decode frame;
  this is causal removal evidence, not a broad latency claim.
- Gates: independent focused and full `pkg/analysis` plus `cmd/bv` tests passed;
  delegated build/vet/formatting/whitespace gates passed. UBS retained only a
  pre-existing registry timeout heuristic outside changed lines.
- Shared-tree note: a concurrent peer committed and pushed the exact candidate
  bytes as `5b73a6b6` while verification was running. The commit was inspected
  and preserved; it was not amended or duplicated.
- Result: PRODUCTIVE runtime optimization. No cache-codec, cascade, or broad
  end-to-end speedup claim.
### Pass 7 — Insights-TransitiveSimulation — zero change
- Source and behavior review covered chain, diamond, multiple-prerequisite,
  cycle, duplicate, nil, missing-ID, and non-blocking-edge cases. Focused tests
  passed without changing production or test code.
- Low-load evidence: the exact current 540-issue registry command took 0.13 s
  wall and produced a 90 ms CPU profile containing neither `TopWhatIfDeltas`
  nor `countTransitiveUnblocks` as a named frame. An intentionally isolated
  batch benchmark measured about 6.96 ms/op, 2.94 MB/op, and 18,252 allocs/op;
  the transitive counter itself was about 1.45% of sampled CPU, roughly 85 us
  per 540-candidate batch. The isolated benchmark overstates the registry path
  because the latter already reuses completed stats.
- Decision score: impact 1/5 x confidence 4/5 / effort 3/5 = 1.33, below the
  campaign threshold.
- Result: ZERO CHANGE (convergence count 1). Retry only if an admissible target
  profile attributes at least 5% CPU or 1 ms/invocation to the transitive
  simulation, or a representative larger/deeper graph promotes it into the
  top five frames.
### Pass 8 — Insights-MapLimitTopK — zero change
- Exact current source and 540-issue data hashes matched the low-load worker.
  Source inspection found eight complete 540-entry defensive copies, eight
  sorts, and eight 200-entry result maps in full-stats assembly. The sortable
  slices alone are about 104 KB, but neither the accepted pre-repair profile nor
  the current residual profiles identify this assembly as a material hotspot.
- Existing zero-copy iterators remove only the defensive copy; existing rank
  maps are ID lookups rather than ordered top-k views. A safe replacement must
  preserve score-descending, ID-ascending ties in both current implementations.
- No fresh timing was credited: after synchronization/build, the worker's
  1-minute load was 14.01 and 5-minute load was 10.38, both outside the gate.
- Result: ZERO CHANGE (convergence count 2). Retry only if a low-load registry
  CPU/allocation profile attributes the copy/limit/sort path to at least 5% CPU
  or 1 ms/invocation, or a representative 5,000+ issue workload attributes at
  least 5% of command allocations/time to this assembly.
## Convergence disposition
Passes 7 and 8 are consecutive evidence-backed zero-change passes. The binding
`repeatedly-apply-skill` stop rule therefore ends this campaign at 8/50; 50 was
a hard cap, not a requirement to manufacture 50 mutations. The next action is
the independent final gauntlet, not another optimization pass.
## Final gauntlet
- Revision under test: `1632071f34a05b17208f66799077f2777c05340d`
  plus this evidence-only closure update; RCH source snapshot
  `beads_viewer/2f64b06266de270c`.
- `git diff --check` and `gofmt -l` across all 16 changed Go files passed.
- RCH `go build ./...` and `go vet ./...` passed.
- RCH full `go test ./... -count=1` and `go test ./... -race -count=1`
  passed on non-root `hz1` with browser/test guards. `TMPDIR=/tmp` kept test
  fixtures outside the mirrored repository, and `GOFLAGS=-buildvcs=false`
  accounted for RCH's intentional omission of `.git`.
- Earlier full-suite attempts without those mirror compensations are rejected,
  not green: upward discovery found the mirror's real `.beads`, root execution
  invalidated a permission-denied test, and VCS stamping lacked `.git`.
- Independent profile plant: the baseline produced valid robot JSON plus a
  zero-byte profile; current produced valid JSON plus a 2,789-byte profile
  readable by `go tool pprof` as `Type: cpu`.
- Independent baseline/current differential oracle used the same 540-issue
  JSONL SHA-256 `17b9bf86204f3f637072635bc78e5cb6435a7c14512d3a6210416c28d5216aa9`
  with caches and browser behavior disabled. Normalized hashes matched:
  insights `b7f17bf0a6797ee4cba70ea6810f1ee55ba8609bcedd71e6fd3cae17be7d47c3`,
  plan `b774ad02366ee9dd73d04dd556d413094db21a2034fa56e2a8e402e65129f7ba`,
  next `996ebe42693718697a57fd99fd3e09667aeaaac7adbde86ccb38746225ebb8f6`,
  and triage `bf5a3922e33b03e1b00421c1ef13626edd4b0dd1e03c3971c2b14a908c3d0e49`.
  Normalization removed timestamps/metric timings; triage also rounded
  time-derived floats to 12 decimals. Its raw residual was only compute time
  plus approximately 1e-14 churn/risk/score drift.
- Exact-file UBS scanned all changed Go files and exited 1, so it is not called
  clean. It reported 188 critical, 18 warning, and 1,693 informational heuristic
  matches. Review found no concrete changed-line defect; displayed timeout/timer
  findings were pre-campaign and already canceled/stopped, while the new
  benchmark cancellation is invoked immediately.
## Anti-ceremony and honesty audit
- USER work: `854a8070` made a previously broken profiling capability real;
  `4b685960` and `5b73a6b6` removed measured repeated analysis from the robot
  insights path. ENABLER work: `e4a1d7ac` and `19889a77` repaired contaminated
  measurement seams. PROCESS work is limited to the revision-bound ledger and
  progress evidence in `71ac37bd`, `1632071f`, and this closure.
- The immediate demo is concrete: the planted 540-issue what-if batch moved
  from 4.4108 s / 1.648 GB / 8,530,586 allocations to 3.203-16.564 ms /
  1.444-4.931 MB / 8,504-26,341 allocations while normalized robot output
  stayed identical. This is a same-worker microbenchmark result, not a broad
  command-latency promise.
- No tests were weakened, replaced with mocks, or made less mutation-sensitive;
  no goldens or dependencies were regenerated; no cited stderr was suppressed.
  `DisableCache` is `json:"-"` and tested absent from robot schema.
- One intermediate cache-benchmark assertion was removed because cache decode
  does not preserve elapsed timing fields; value, readiness, graph-size, and
  score assertions remained. Incorrect flat-copy results, high-load profiles,
  a zero-byte `perf` capture, stale session-index absence claims, RCH exit-143,
  and the initial mirror-contaminated full-suite runs all received no credit.
- A concurrent peer's exact pass-6 commit was inspected and preserved rather
  than rewritten. The untracked Beads lock was never touched. No Beads issue
  was claimed or closed because authoritative Beads/Agent Mail state was
  unavailable.
- Proof boundary: source/build/test/race, deterministic robot-output, remote
  low-load profiling, and synthetic microbenchmark evidence only. No deployed,
  browser, soak, release, or complete low-load Git-correlation claim.
# 2026-08-24 continuation — fresh 50-pass cap
Skill: `extreme-software-optimization`, orchestrated serially by
`repeatedly-apply-skill`. The completed 2026-08-23 passes remain prior evidence
and do not count toward this continuation. The 50 domain-specific missions
already enumerated above remain the mission pool; rejected candidates stay
retired until their recorded retry predicates become true. Two consecutive
evidence-backed zero-change passes still end the loop before the cap.
## Status: COMPLETE — converged after pass 3 of at most 50
## Minimal process-artifact gate
- Consumer: the operator and the next serial pass agent.
- Gate: no performance change or speedup claim proceeds without a pinned
  workload, raw samples, behavior oracle, and retry predicate for rejected work.
- Observed defect class: the previous campaign found contaminated cache
  benchmarks, high-load measurements, a zero-byte CPU profile, and a Git-less
  mirror incorrectly offered as broad evidence.
- Retirement: this section becomes historical at the final gauntlet or is
  superseded when runtime source or workload fingerprints change.
## Fresh profile — complete Git-backed `--robot-triage`
- Runtime revision: `1632071f34a05b17208f66799077f2777c05340d`; current
  `07bd43eb` differs only in the two evidence files, not Go/runtime bytes.
- Host/toolchain: `hz2`, Linux/amd64, Go 1.25.5. Fresh 1-minute load stayed
  between 5.56 and 6.67 during the credited profile and sampling window.
- Fixture: explicit `.beads/issues.jsonl`, SHA-256
  `17b9bf86204f3f637072635bc78e5cb6435a7c14512d3a6210416c28d5216aa9`;
  540 loaded issues, 19 open, complete Git history, `history_status=ok`.
- Cold application-cache baseline, 20 serial samples: mean 0.779325 s,
  p50 0.781806 s, p95 0.842383 s, worst observed 0.871028 s, CV 8.62%.
  Sample count is sufficient only for an advisory p95/worst-observed tail, not
  a tight p99 estimate.
- Warm triage, 100 samples: p50 0.098713 s, p95 0.119586 s, CV 17.12%.
  Warm `--robot-next`, 100 samples: p50 0.074563 s, p95 0.102660 s,
  CV 22.09%. Both are too noisy for a speedup claim.
- Cold `/usr/bin/time -v`: 0.86 s wall, 0.65 s user, 0.64 s system,
  109,144 KiB peak RSS.
- Cold CPU profile: 530 ms of samples; `GenerateReportCached` 360 ms
  cumulative, `extractHistoryArtifact` 330 ms, snapshot extraction 310 ms,
  streamed blob reads 190 ms, large allocation 100 ms, memory clearing 70 ms,
  record-line-set construction 60 ms, and background GC 70 ms.
- Cold `strace -f -c`: eight `execve` calls plus 13,503 reads and 15,439
  writes. Summed-across-threads futex/wait totals are attribution evidence, not
  wall-clock durations.
- Behavior oracle: `SOURCE_DATE_EPOCH=1787540400`; comparisons remove only
  `generated_at`, `compute_time_ms`, and per-metric `ms`/`elapsed_ms` fields.
  Raw runs differed only in those measured timing fields.
- Preserved remote evidence root: `/tmp/bv-perf-20260824.z4mtLg`.
## Ranked opportunities
| Rank | Candidate | Profile support | Impact | Confidence | Effort | Score |
|---:|---|---|---:|---:|---:|---:|
| 1 | Reuse large blob buffers after their record sets are evicted | 100 ms large allocation + 70 ms clearing + 70 ms GC under snapshot extraction | 3 | 5 | 3 | 5.0 |
| 2 | Reduce record-line-set hashing/entry allocation | 60 ms cumulative in `newRecordLineSet` | 2 | 4 | 3 | 2.67 |
| 3 | Change report-cache codec/layout | Not a cold-extraction top frame in this profile | 1 | 2 | 3 | 0.67 |
Only rank 1 enters pass 1. The warm paths and lower-ranked candidates receive
no mutation credit without a new admissible profile.
## Continuation pass log
### Pass 1 — Correlation-BlobBufferReuse — `22305d12`
- Profile citation: the pinned cold Git-backed triage profile attributed
  100 ms to large allocation, 70 ms to memory clearing, and 70 ms to
  background GC beneath snapshot extraction.
- Change: snapshot record sets now retain their backing blob explicitly. At
  the existing last-use eviction point the set is removed before its buffer is
  returned to a one-slot, largest-capacity spare in `blobReader`. The next
  valid blob can reuse that storage; missing objects do not consume the spare.
- Planted negative: `TestBlobReaderReusesOnlyRecycledBuffers` proves two live
  blobs never alias, deliberately recycled storage is reused, the other live
  blob stays byte-identical, claimed/closed events remain exact, and a missing
  object leaves the spare intact. A four-snapshot Git differential test forces
  equal-capacity reuse across overlapping history and matches the legacy patch
  extractor byte-for-byte for created/claimed/closed/reopened events.
- Behavior proof: baseline and candidate normalized triage output shared
  SHA-256
  `fb437df32493bde94019d8c9aa784a8681841545ffa4faa03337fb14e9d279f4`.
  The fixture remained 540 issues / 19 open with `history_status=ok` and source
  JSONL SHA-256
  `17b9bf86204f3f637072635bc78e5cb6435a7c14512d3a6210416c28d5216aa9`.
- Resource proof on low-load `ovh-b`: 20 interleaved cold-cache pairs without
  GC tracing reduced mean user CPU from 0.7825 s to 0.6665 s (-14.8%); the
  candidate was lower in 19 pairs with one tie (exact one-sided sign
  p=0.0000019, CV 5.47%/4.11%). Mean system CPU fell 7.9%. Ten traced pairs
  reduced mean GC cycles from 24.8 to 12.0 (-51.6%). Five merged CPU profiles
  reduced samples from 2.53 s to 2.06 s (-18.6%), with `memclr` samples down
  62% and GC scan cumulative samples down 41%.
- Explicit non-claims: high-resolution command wall time did not improve
  defensibly. A 20-pair batch showed only -0.85% mean with p95 and worst
  slightly worse; a 40-pair batch showed -0.83% mean, an even 20/20 pair split,
  and worse median/p95/worst. Peak RSS was effectively flat but slightly higher
  (+0.106%). This pass claims CPU/GC reduction only, not latency or RSS.
- Gates: exact-file UBS found zero critical issues; its lone warning was the
  shadow scan's missing `go.mod`, not a source finding. Repository-owned Go
  files were formatted, `git diff --check`, full build, and full vet passed.
  The direct low-load candidate suite passed every package except one loader
  test whose tarball intentionally lacked `.git`; that exact package then
  passed in a Git-backed candidate copy. The RCH full-suite false reds remain
  negative evidence rather than green credit.
- Result: PRODUCTIVE runtime resource optimization. Convergence count resets
  to zero. The post-change profile ranks record-line-set hashing/allocation as
  the next candidate; it must independently clear the same keep gate.
### Pass 2 — Correlation-RecordLineSetInlineEntries — zero change
- Profile citation: after pass 1, five merged profiles attributed 0.55 s of
  2.06 s total samples (26.7% cumulative) to `newRecordLineSet` and 0.26 s
  flat (12.6%) to AES-backed map hashing.
- Candidate tested: pre-size from JSONL line count, store primary entries inline
  instead of behind one allocation per unique record, and use lazy exact-line
  collision buckets. A forced-collision/duplicate test, the pass-1 buffer test,
  the temporary-Git overlapping-history differential, full correlation tests,
  build, vet, formatting, whitespace, and exact-file UBS all passed.
- Cost exposed before acceptance: capacity planning added one full blob scan and
  collision correctness added exact-line comparison over unchanged records—on
  this history, roughly 275 MB of additional sequential reads for each pass.
- Rejection proof on low-load `ovh-b`: 20 interleaved cold-cache pairs, with
  both side CVs below 6%, regressed mean wall from 0.759014 s to 0.801633 s
  (+5.62%), median from 0.770471 s to 0.806058 s (+4.62%), p95 from
  0.791458 s to 0.855642 s (+8.11%), and worst observed from 0.805721 s to
  0.861852 s (+6.97%). Baseline won 15/20 pairs (one-sided sign p=0.0207).
  Mean user CPU also rose from 0.6575 s to 0.7015 s (+6.69%). RSS was flat.
- Behavior proof: normalized baseline/candidate robot output remained exact at
  `32aaadc67dd872e359d903863c36dc48cbc9ad7f113487129f3ce3b8b0aad10f`,
  with 19 open issues and `history_status=ok`.
- Disposition: REJECTED and restored exactly to `54b4db4a`; no production or
  test diff remains. The preserved remote evidence root is
  `/tmp/bv-ab2-20260824.YPD5lq`.
- Result: ZERO CHANGE (convergence count 1). Retry only with a design that
  avoids both a separate capacity scan and full-line equality on the ordinary
  path, then require non-regressing wall tails as well as lower CPU/allocation.
### Pass 3 — Correlation-ReportCacheCodecLayout — zero change
- Materiality gate: the accepted post-pass-1 denominator was 2.06 s of merged
  CPU samples, so the 5% threshold was 103 ms. No report-cache or codec frame
  appeared at that level or at the alternate 1 ms/invocation threshold; the
  opportunity score remained 0.67, below the 2.0 mutation gate.
- Source proof: the outer cache already uses `goccy/go-json`, avoids rewriting
  on read hits, bounds itself to six entries, expires after 24 hours, and caps
  serialized reports at 64 MiB. Preserved one-entry artifacts measured
  772,000 bytes for the report cache and 673,954 bytes for the HEAD artifact.
- A full miss does serialize a report once for the size ceiling and again as
  part of the complete cache file, but the cold profile attributes no material
  cost to that lever. Changing codec or layout would therefore be speculative.
- Result: ZERO CHANGE (convergence count 2). Retry only when a representative
  profile attributes at least 5% sampled CPU or 1 ms/invocation to a specific
  report-cache read/write/codec function, including on a substantially larger
  multi-entry cache. No files or test gates changed in this pass.
## Continuation convergence disposition
Passes 2 and 3 are consecutive evidence-backed zero-change passes. The binding
`repeatedly-apply-skill` rule ends the continuation at 3/50; 50 is a cap, not a
mutation quota. Runtime remains `22305d12`. Pass 2's candidate was restored
exactly, and pass 3 made no edits.
## Continuation final gauntlet
- Independent verdict at frozen `12afb2d2`: ACCEPT runtime `22305d12`; PARTIAL
  campaign closure because this pass-3 disposition had not yet been committed
  while the verifier held its snapshot. This evidence-only closure resolves
  the documentation gap without changing runtime bytes; the verifier did not
  independently reproduce pass 3's materiality audit from a reopenable profile.
- Independent low-load replay on the same 540-issue/19-open Git fixture used
  ten interleaved cold-cache pairs. Mean user CPU moved from 0.647 s to
  0.573 s (-11.44%), with 10/10 candidate wins and exact sign p=0.00098.
  Five traced pairs reduced mean GC cycles from 25.8 to 13.0 (-49.61%).
- Independent non-claims: wall mean moved from 0.679239 s to 0.663896 s
  (-2.26%), but only 8/10 pairs favored the candidate and sign p=0.0547. Peak
  RSS increased from 108,846.4 KiB to 109,039.6 KiB (+0.177%). No wall or RSS
  improvement is claimed.
- All 20 independent normalized robot outputs matched at SHA-256
  `5992ff99901b1c5abf8ccf3b1a9e3d2490a6f0eda1742cad938e7b3ff9809918`.
  Normalization removed only generated timestamps and measured timing fields;
  the fixture retained 19 open issues and `history_status=ok`.
- Independent correctness proof passed focused buffer/lifetime tests, full
  `pkg/correlation`, the real-fixture legacy/snapshot differential at
  1,776/1,776 events, full ordinary `go test ./...`, build, vet, repository-owned
  formatting, whitespace checks, exact-file UBS, and correlation race.
- Full aggregate race remains uncredited: RCH lacked a GUI editor for two UI
  tests; a copied low-load tree lacked repository metadata for one loader test;
  and the exact local tree ran above the load gate and hit one five-second CLI
  timeout. All unaffected packages passed, but these results are negative
  environment evidence rather than a clean aggregate race gate.
## Continuation anti-ceremony and honesty audit
- USER work: `22305d12` reduces real cold-history CPU/GC work and includes
  causal lifetime/differential tests. ENABLER work: the initial Git-backed
  profile and repaired profile lifecycle/benchmark seams inherited from the
  prior campaign. PROCESS work: only the existing progress/negative-evidence
  files, whose consumer, gate, observed defect class, and retirement condition
  were recorded before mutation.
- The ledger prevented three false wins: no broad wall-latency claim for pass 1,
  no RSS claim, and no retention of pass 2 despite exact output and green tests.
  Pass 3 stopped rather than manufacturing a codec change.
- No test was weakened, skipped for credit, regenerated, or replaced by a mock;
  no dependency, schema, timeout, tracker state, or browser behavior changed.
  The untracked Beads lock was never touched.
- These evidence sections retire as active process artifacts now. Reopen only
  when runtime source or workload fingerprints change, or a retry predicate in
  the negative ledger becomes true.
- Proof boundary: source/build/test/correlation-race, deterministic robot output,
  and low-load Git-backed CPU/GC evidence. No broad latency, RSS, full-race,
  deployed, browser, release, or soak claim.
## 2026-08-24 profile-triggered reopen

The existing evidence files were reopened because pass 3's explicit retry
predicate became true. On pinned `hz2`, five merged cold-cache profiles of the
accepted runtime attributed 190 ms of 2.63 s (7.22%) to the two correlation
cache put/write paths. No new process artifact was created; the existing
progress, hotspot, and negative-evidence files retain the same consumer,
claim gate, and retirement condition.

Fresh baseline evidence used Go 1.25.5, Linux/amd64, the 540-issue/19-open
Git-backed fixture at SHA-256
`17b9bf86204f3f637072635bc78e5cb6435a7c14512d3a6210416c28d5216aa9`,
and a new application cache per invocation. Twenty runs measured mean user CPU
0.485 s (CV 8.68%) and mean peak RSS 109,128.8 KiB (CV 0.14%). Wall CV was
10.79%, so wall remains advisory only. Five GC traces averaged 12 cycles. All
baseline, trace, and syscall-trace outputs normalized to
`79b671e20f094b62cc1b4250ba50e39d89df6b0ee9c29b177e79e925c1a5ee1a`.
`strace -f -c` observed 14,115 reads, 15,434 writes, and eight `execve` calls;
summed futex/wait time is attribution evidence, not elapsed time. Preserved
fresh-profile evidence root: `/tmp/bv-fresh2-20260824T014700Z`.

| Candidate | Impact | Confidence | Effort | Score | Disposition |
|---|---:|---:|---:|---:|---|
| Reuse size-checked cache JSON via raw payloads | 2 | 5 | 2 | 5.0 | Enter pass 4 |
| Rework record-line-set allocation again | 3 | 2 | 4 | 1.5 | Prior negative controls still bind |
| Avoid snapshot blob reads/Git pipe traffic | 4 | 2 | 5 | 1.6 | Below mutation gate; semantic/cache redesign |

### Pass 4 — Correlation-CacheRawPayloadReuse — rejected, zero change

- Candidate: retain the size-checked artifact/report bytes as
  `json.RawMessage` in the version-1 cache envelope, then decode only the
  selected entry on hits. Focused tests proved two-entry retention and exact
  round-trip behavior. A planted 16-byte ceiling exercised the real put paths:
  oversized artifact/report keys remained misses and prior valid entries
  survived. Null payloads also remained misses.
- Behavior proof: all 40 unprofiled and ten profiled candidate/baseline outputs
  normalized to the same SHA-256
  `79b671e20f094b62cc1b4250ba50e39d89df6b0ee9c29b177e79e925c1a5ee1a`.
- End-to-end rejection on low-load `vmi1227854`: 20 alternating cold-cache
  pairs had every pre-run one-minute load below 10. Mean user CPU moved only
  0.4685 s to 0.4645 s (-0.85%); candidate won 11, tied one, and lost eight
  pairs (one-sided sign p=0.3238). Both user-CPU CVs exceeded 17%, so no CPU
  improvement is publishable. Wall was noisier still and mean RSS increased
  0.129%, with the candidate higher in 16/20 pairs.
- Target-level rejection: five merged profiles moved the focused cache path
  from 70 ms/1.75 s (4.00%) to 90 ms/1.81 s (4.97%). `RawMessage` avoided the
  second structural traversal but the enclosing encoder still validated and
  compacted/copied the payload; the measured target did not improve.
- Disposition: REJECTED. Production and test bytes are restored exactly to
  pre-candidate `19fb9f06`; the unrelated Beads lock remains untouched. The
  accidentally committed conflict marker in this file was removed. Preserved
  evidence roots: `/tmp/bv-ab4-results-122` and `/tmp/bv-ab4-prof-122`.
- Result: ZERO CHANGE (convergence count remains two, following pass 3). The
  binding repeated-skill stop rule ends this reopen at 4/50; 50 remains a cap,
  not a quota. Retry only with a serializer/layout that reuses preflight bytes
  without a second validation/compaction copy, and require a lower focused
  profile plus statistically defensible non-regressing end-to-end resources.

## Profile-reopen final gates

- Final runtime files match pre-candidate `19fb9f06` exactly: disk cache
  `ad1add7003d235ea5114796cfcb8221ab9ae8a27a3f2101d6c0e2ccfd347326a`,
  HEAD-artifact cache
  `e7bd61627e7b185378891debbca20473450cffee16277de2667ed1954623da92`,
  cache test
  `2e4cd8d707ff6398fe06d51ff6f7d9a17b2ed2ec796ff15069325c4ea60d9e51`,
  and codec probe
  `8d3a772fc1076b496b243ccfb7449453e9ef19cff41e97c4da8bb2e62fb3b1c0`.
- Exact-file UBS found zero critical issues; its sole warning was the shadow
  scan's missing `go.mod`, not a source finding. Repository Go formatting,
  `git diff --check`, full build, and full vet passed.
- Focused correlation tests passed. The first full ordinary suite attempt had
  one intermittent `TestRace_DataConsistency` raw-output mismatch. Five focused
  replays passed, and the one allowed full-suite retry passed every package,
  including E2E. The initial flake remains negative evidence.
- The full aggregate race suite passed every package, including E2E, as
  non-root `ubuntu` in a complete Git-backed tree with Go 1.25.5,
  `BV_NO_BROWSER=1`, `BV_TEST_MODE=1`, and pre-run load 5.19. This satisfies
  NE-20260824-10's retry predicate rather than erasing its earlier failures.
- Anti-ceremony inventory: the restored production bytes are USER work because
  they prevent a measured regression; the focused negative tests were valid
  ENABLER work but were removed with the rejected implementation; updates to
  the three pre-existing evidence files are PROCESS work with the already
  recorded consumer and retirement gate. No new harness, dependency, schema,
  timeout, tracker mutation, browser action, or file deletion was introduced.
- Honesty boundary: the campaign retains only the already accepted blob-buffer
  CPU/GC win. Pass 4 adds no speedup. No wall-latency or RSS improvement is
  claimed; no deployed, release, browser, or soak evidence was run.
## Profile-reopen independent verifier

- Verdict: ACCEPT at frozen `495bdf673d966cf6d4408e64ebb43a628729aaaf`;
  start/end HEAD and tracked tree remained identical, with only the preserved
  untracked Beads lock present.
- The verifier independently confirmed exact `19fb9f06` runtime/test identity,
  absence of the rejected helpers and conflict markers, all 50 normalized
  behavior hashes, the paired user-CPU/RSS math, and the focused profile
  regression from 4.00% to 4.97%.
- Provenance limitation: the preserved remote result directories contain raw
  timings, outputs, and profiles but no separate per-run load log. The verifier
  confirmed the math but could not independently reconstruct every pre-run
  load sample; it correctly declined ceremonial local reruns at load 149.31.
- This documentation-only verifier receipt does not change runtime bytes or the
  pass-4 zero-speedup disposition.

## Operator-mandated full-50 resumption

The operator explicitly overrode the earlier two-zero-pass stopping condition
and requires all 50 iterations in this active 2026-08-24 continuation. Passes
1-4 above count; work resumes at pass 5 and will proceed through pass 50. A
full iteration still may reject or defer a candidate when evidence loses; the
override removes early stopping, not the profile-first or no-regression gates.
`alien-graveyard` and `alien-artifact-coding` now augment candidate discovery.
Here `/dp` is the operator's `~/projects`, concretely
`/Users/jemanuel/projects`; the canonical graveyard is therefore
`/dp/alien_cs_graveyard`. Candidates are grounded in that corpus, the target
code, and current profiles.

## Status: IN PROGRESS — Pass 29 of 50

### Pass 5 — Correlation-RecordLineSetInlineValues — rejected, zero change

- Profile citation: the accepted fresh profile attributed 0.42 s / 15.97% to
  `newRecordLineSet`, with material GC, `memclr`, and map-hash work. This pass
  tested the narrower NE-20260824-08 retry: inline values only, with no
  capacity pre-scan, collision buckets, or ordinary-path line comparison.
- Rewrite certificate `R-BV-005`: for each hash `h`,
  `M(h) = sum(1 for line where hash(line)=h)` is unchanged by replacing a
  pointer-valued map entry with a copied-and-reassigned value. The planted
  duplicate case substituted `M_old(h)=2`, `M_new(h)=1` and required exactly
  one removal; its inverse required exactly one addition. Existing recycled-
  buffer tests retained the alias-lifetime negative.
- Behavior proof: every one of 60 unprofiled paired outputs, 30 GC-traced
  outputs, and 16 profiled outputs normalized to SHA-256
  `79b671e20f094b62cc1b4250ba50e39d89df6b0ee9c29b177e79e925c1a5ee1a`.
  Focused tests and build passed under exact Go 1.25.5 on the low-load worker.
- End-to-end evidence: 30 alternating cold-cache pairs had pre-run load at
  most 6.79. Mean user CPU moved 0.982 s to 0.975 s (-0.71%), with 16 wins and
  14 losses (sign p=0.4278); the paired-bootstrap 95% improvement interval was
  [-7.33%, +7.99%]. Mean wall changed +0.10%, and mean RSS changed +0.018%.
- Target evidence: 15 traced pairs moved mean GC cycles 12.53 to 11.93
  (-4.79%; 8 wins, 5 ties, 2 losses; sign p=0.0547). Eight merged profiles
  moved `newRecordLineSet` 0.65 s to 0.57 s cumulative, but map assignment/
  access and `memclr` increased. The target changed shape without clearing the
  product-resource gate.
- Decision card: `keep iff lower95(user_cpu_gain) >= 3% AND wall_p95_delta <= 0
  AND rss_regression <= budget`. Substitution gives `-7.33% >= 3%`, false.
  Assumptions were identical Go/source/data, alternating order, and load <10;
  a pointer-free packed-offset representation is the counterfactual that may
  change this decision.
- Harness negative evidence: an initial shell loop failed before sampling
  because its order string was not split; it attempted a nonexistent binary.
  That empty-output artifact was preserved and receives zero measurement
  credit. The explicit-label v2 harness produced the results above.
- Disposition: REJECTED. Both edited files were restored byte-for-byte to
  pre-pass hashes `b3bebd3d...` and `cec1147e...`; only the existing Beads lock
  remains untracked. Preserved evidence root:
  `/tmp/bv-p5-20260824.76DIJ4` on `vmi1156319`.

### Pass 6 recommendation contract — Correlation-BlobReaderDirectPayload

- Change/hotspot: shrink only `newBlobReader`'s 10 MiB `bufio.Reader`; keep
  `readFull`'s large destination. `blobReader.read` is 0.73 s / 27.76% of the
  accepted profile. Graveyard mapping: communication avoidance, cache-shaped
  I/O, and the section 16.7 constants/crossover guard.
- Scores/tier: extreme score `(5*5)/2 = 12.5`; graveyard EV
  `(4*5*3)/(2*1) = 30`; relevance 4.8/5; Tier A experiment.
- Wedge/budget/fallback: one constructor constant, no API/schema change; one
  live payload plus the existing one-spare bound remains. Any malformed,
  truncated, missing-object, or child-process error follows the current path.
  Restore the 10 MiB reader immediately if CPU/allocation does not improve or
  syscall/wall tails regress.
- Expected loss/calibration: compare 64 KiB versus 10 MiB under loss
  `CPU + tail-regression penalty + RSS penalty`; accept only with at least 3%
  defensible user-CPU improvement and non-regressing p50/p95/p99-equivalent
  observed tails/RSS. Alternating low-load pairs and merged profiles calibrate
  the decision; no online adaptive policy is introduced.
- Isomorphism/negative proof: exact Git object bytes, object boundaries, event
  order, normalized robot output, and error behavior; exercise small and
  >64 KiB payloads, back-to-back objects, missing/truncated/malformed protocol,
  and live-buffer aliasing. Current implementation is the comparator/rollback.
- Repro/provenance: exact Go/source/data hashes, per-run load log, raw JSON,
  stderr, timings, profiles, and syscall summary go in one remote evidence
  root. Primary-paper/legal status is N/A (Go standard-library buffering
  behavior); no composed controller or production demo is involved.

### Pass 6 — Correlation-BlobReaderDirectPayload — rejected, zero change

- Candidate: reduce only `newBlobReader`'s retained input buffer from 10 MiB
  to 64 KiB. Go 1.25.5's `bufio.Reader.Read` then reads a large empty-buffer
  request directly into `readFull`'s destination. Focused tests covered the
  exact buffer choice, small and 128 KiB back-to-back payloads, recycled-buffer
  aliasing, malformed headers/sizes, truncation, and missing trailers.
- Behavior proof: all 60 unprofiled paired outputs, 30 GC-traced outputs, and
  16 profiled outputs normalized to SHA-256
  `79b671e20f094b62cc1b4250ba50e39d89df6b0ee9c29b177e79e925c1a5ee1a`.
  Focused and full correlation tests plus the candidate build passed under
  exact Go 1.25.5. No race result is claimed because the remote wrapper fell
  back to a local execution that was stopped.
- End-to-end evidence: 30 alternating cold-cache pairs preserved only runs
  whose fresh pre-run one-minute load was below 10. The harness correctly
  stopped at 10.26 before candidate pair 15, then resumed after load fell.
  Mean user CPU moved 0.8573 s to 0.8160 s (-4.82%), but wins/ties/losses were
  15/1/14 and the paired-bootstrap 95% improvement interval was
  [-2.17%, +11.50%]. Mean wall improved 1.09% with 14/16 wins/losses; mean RSS
  improved only 0.031%. None is a defensible product-resource win.
- Target evidence: eight merged profiles showed the intended mechanism:
  `memmove` fell from 0.48 s to 0.26 s (-45.8%) and `NewReaderSize` fell from
  0.08 s to 0.01 s. The saved 10 MiB allocation also lowered the heap goal,
  however: 15 paired traces increased mean GC cycles from 12.33 to 15.87
  (+28.65%), losing all 15 pairs, while background-GC samples rose from
  0.45 s to 0.62 s.
- Syscall boundary: two `strace -f -c` runs per side left writes and `execve`
  effectively unchanged, while mean reads increased from 13,619 to 13,709.
  Total syscall counts were mixed, so no I/O win offsets the GC regression.
- Decision card: `keep iff lower95(user_cpu_gain) >= 3% AND gc_cycles <=
  baseline AND tails/RSS do not regress`. Substitution gives `-2.17% >= 3%`
  and `15.87 <= 12.33`, both false. The causal rewrite worked, but its heap-
  pacing side effect dominated often enough to reject this formulation.
- Disposition: REJECTED. Runtime and tests were restored byte-for-byte to
  `b3bebd3d...` and `cec1147e...`; only the pre-existing Beads lock remains
  untracked. Preserved evidence root: `/tmp/bv-p6-20260824.RwmCIT` on
  `vmi1156319`. Retry only after a separately justified heap-pacing change or
  with a buffer-size policy that retains the pacing benefit and proves no GC
  regression.

### Pass 7 recommendation contract — Correlation-RawDiffAllocationElision

- Change/hotspot: replace `parseRawDiffLines`' per-commit 64 KiB scanner
  allocation and string-token conversion with a bounded byte parser that
  consumes the already-materialized Git output directly. Current histories
  invoke it once per followed commit; 498 representative commits imply about
  31.1 MiB of transient scanner buffers before token/string work.
- Scores/tier: extreme score `(4*5)/2 = 10`; graveyard EV
  `(4*5*3)/(2*1) = 30`; relevance 4.7/5; Tier A experiment.
- Rewrite certificate `R-BV-007`: for input byte sequence `B`, both parsers
  must emit the identical ordered sequence of `(oldOID,newOID)` pairs for
  every valid `:... status` record and reject/ignore exactly the same malformed
  forms. Existing parsing is the executable comparator and rollback.
- Negative proof: planted fixtures cover empty input, multiple records,
  rename/copy records with path trailers, abbreviated or malformed OIDs,
  missing status/tab separators, CRLF, and a record larger than 64 KiB. Any
  semantic difference, allocation non-win, or product-resource regression
  restores the original parser.

### Pass 7 — Correlation-RawDiffAllocationElision — rejected, zero change

- Candidate: replace the per-commit 64 KiB `bufio.Scanner`, whole-line string,
  and `strings.Fields` slice with a bounded direct byte parser. An embedded
  copy of the old parser served as the executable oracle. The differential
  corpus covered empty/noisy/malformed input, rename/copy trailers, abbreviated
  OIDs, ASCII and Unicode whitespace, CRLF, pre-populated state, deletion
  short-circuiting, a 96 KiB record, and exact terminated/unterminated 10 MiB
  token boundaries. A planted null-new-OID mutation failed 14 cases.
- Target proof: on exact Go 1.25.5, the valid five-sample one-record benchmark
  reduced median time from 66.949 us to 1.447 us (46.3x), allocation from
  65,920 to 224 B/op (-99.66%), and allocations from five to three. The
  many-noise median moved 143.022 us to 5.755 us, with 68,992 to 224 B/op and
  133 to three allocations. At 498 followed commits, the ordinary formulation
  therefore removed about 31.2 MiB of transient scanner capacity.
- Product proof: 100 alternating cold-cache pairs on quiet `vmi1149989` used
  the exact 540-issue fixture and had fresh pre-run one-minute load 0.43-1.63.
  All 200 outputs normalized to SHA-256
  `79b671e20f094b62cc1b4250ba50e39d89df6b0ee9c29b177e79e925c1a5ee1a`.
  Mean user CPU moved 0.3913 s to 0.3858 s (-1.41%), with a paired-bootstrap
  95% improvement interval of [-1.17%, +4.22%] and wins/ties/losses 46/14/40.
  Median and p95 were unchanged. Wall was exactly flat (+0.02% candidate),
  while mean RSS improved only 0.016%; neither is a win.
- Causal secondary evidence: 15 paired traces reduced mean GC cycles from
  11.8 to 9.8 (-16.95%), with 13 wins, two ties, zero losses, one-sided
  p=0.000122. Eight merged CPU profiles totaled 2.34 s baseline versus 2.08 s
  candidate, but the old `parseRawDiffLines` itself held only 0.02 s cumulative
  samples. Two syscall traces per side kept seven `execve` calls and about
  15,320 writes; read counts did not regress directionally.
- Decision card: `keep iff lower95(user_cpu_gain) >= 3% OR a primary product
  resource improves materially with no CPU/wall/tail regression`. Substitution
  gives `-1.17% >= 3%`, false. Fewer GC cycles are a mechanism metric, while
  wall is flat and sys-time p95/p99 worsen; the alternative clause is also
  false. A spectacular microbenchmark does not justify 94 extra production
  lines on this workload.
- Harness negatives: the first independent build hit a shared remote `$WORK`
  cache race and received zero credit; a retry with pass-private `TMPDIR` and
  `GOCACHE` passed. The first benchmark reported pre-run load 20.78 and was
  interrupted; its partial output is preserved but excluded. The first four
  pairs on `vmi1156319` stopped correctly at load 10.13 and are likewise not
  mixed into the quiet-host cohort.
- Disposition: REJECTED. Runtime and tests were restored byte-for-byte to
  `b3bebd3d...` and `cec1147e...`; only the pre-existing Beads lock remains
  untracked. Primary evidence root:
  `/tmp/bv-p7-quiet-20260824.O74wMG` on `vmi1149989`; excluded/diagnostic root:
  `/tmp/bv-p7-20260824.uPuMeY` on `vmi1156319`.

### Pass 8 recommendation contract — Loader-AdaptiveTransportBuffer

- Change/hotspot: serial loading currently gives `bufio.Reader` the full
  10 MiB logical line cap even for today's 1,367,658-byte file whose maximum
  line is 10,881 bytes. Separate transport buffering from the unchanged line
  limit: for a serial `*os.File`, clamp `stat size + 1` to [64 KiB, 10 MiB];
  opaque readers start at 64 KiB and assemble only exceptional fragmented
  lines into a reusable cap-bounded scratch. The >=4 MiB parallel path stays
  unchanged.
- Scores/tier: extreme score `(4*4)/3 = 5.33`; graveyard EV
  `(4*4*3)/(3*1) = 16`; relevance 4.6/5; Tier A experiment, activated first by
  a fresh loader allocation/CPU seam rather than assumed from source shape.
- Rewrite certificate `R-BV-008`: if physical fragments `F(i,j)` form logical
  line `L_i`, the candidate must establish `concat_j F(i,j) = L_i` after the
  same LF/CRLF handling. After each logical line, `(line number, ordered issues,
  pooled references, ParseStats, ordered warnings)` must equal the comparator.
  BOM remains first-line-only; EOF partial lines and read errors keep context.
- Negative proof: cap-1/cap/cap+1 with LF, CRLF, and no final LF; CR at a buffer
  boundary; consecutive long records; malformed-warning order; partial-line
  reader errors; and an allocation assertion that resurrecting a 10 MiB
  transport buffer restores roughly 10 MiB/op. Any GC-cycle increase rejects
  the candidate, directly incorporating Pass 6's heap-pacing failure.

### Passes 8-11 — measured rejections, zero runtime change

- Pass 8's smaller adaptive loader buffer reduced B/op 69.52%, but regressed
  direct load 12.11%, cold-triage user CPU 17.00%, wall 6.50%, and GC cycles
  136.48%. Pass 9's nested dependency codec improved direct loading 13.00%
  and allocations 33.63%, but does not run in the dominant correlation parser
  and regressed cold-triage user CPU 2.12%. Both were restored exactly.
- Pass 10's `GOGC=400` experiment improved mean user CPU 5.07%, but its lower
  confidence bound was 2.82%; system CPU rose 16.72% and wall regressed 3.90%.
  No runtime default changed. Pass 11's packed record offsets reduced the
  target's allocations 96.95%, but a valid 50-pair cohort improved user CPU
  only 1.22% and total CPU 0.58%, with intervals crossing zero. The candidate
  was restored exactly.
- Full proofs, negative evidence, and retry predicates are in
  `HOTSPOT_TABLE.md` and `HYPOTHESIS_LEDGER.md`; this progress file records only
  the iteration count and disposition.

### Pass 12 — Correlation-DirectEventFusion — rejected, zero runtime change

- The direct accumulator bypassed synthesized diff buffers and Scanner framing
  while retaining a deterministic legacy fallback. The 1,776-event real Git
  differential was exact; the planted multiplicity mutation failed; focused,
  package, race, build, vet, formatting, and exact-file UBS gates passed.
- In the seed-12012 balanced randomized 50-pair cohort, mean user CPU improved
  6.23% with a 95% interval of +4.21% to +8.18%, and all 100 normalized outputs
  matched. Total CPU improved only 2.98% with a +0.21% to +5.64% interval;
  system CPU regressed 3.25%, and baseline total/wall CV exceeded 10%.
- Result: REJECTED under the predeclared combined-resource gate. The three
  candidate files were restored exactly. Evidence root:
  `/data/tmp/bv-p12-20260824.fusion`.

### Pass 13 — Correlation-OneAheadBlobPrefetch — rejected, zero runtime change

- Eight exact-baseline profiles activated the proposed producer/consumer
  overlap: blob reads held 17.05%, `readFull` 13.36%, and record-set hashing
  16.13% cumulative samples. The bounded future passed object-order, error,
  buffer-lifetime, close-join, race, and 1,776-event differential proofs. A
  delayed-start plant failed the start-before-await test.
- The 12-pair screen improved wall 17.97% with 12/12 wins, but the balanced
  seed-13013 50-pair cohort rejected it: user CPU regressed 6.55% with a wholly
  negative improvement interval, total CPU regressed 6.70%, wall regressed
  0.89%, and wall p95/worst worsened. All 100 normalized outputs matched.
- Result: REJECTED. Per-object goroutine/channel scheduling cost and cache
  interference exceeded the overlap. Both files were restored exactly.
  Evidence root: `/data/tmp/bv-p13-20260824.prefetch`.

### Pass 14 — Runtime-GreenTeaGC — rejected, zero runtime change

- Candidate: build the exact accepted source with Go 1.25.5's
  `GOEXPERIMENT=greenteagc`; no source, dependency, schema, timeout, or runtime
  default changed. Build metadata reports `go1.25.5 X:greenteagc`; candidate
  SHA-256 is
  `fe50a53d9408fbfe175f56ab3680e79a13f40d4cbf0fad3e7e946bbf230fed24`.
- Tooling negative: the remote host's older launcher panicked while resolving
  the experimental toolchain through its FIPS selection path. That attempt
  received zero credit. The credited build invoked the pinned Go 1.25.5
  toolchain directly with `GOTOOLCHAIN=local` and pass-private caches.
- Correctness proof: ordinary and race correlation suites, all-package build,
  and all-package vet passed on the low-load Git-backed worker. Every one of
  the 24 screen outputs and 100 confirmation outputs normalized to SHA-256
  `f8f098b09e0a34b5257ae2f5cfc15da85a99ccc311cb348f6ec1dd9165a75853`.
- The 12-pair screen was only directional: mean user CPU improved 3.54%, total
  CPU 3.49%, and wall 1.78%, with high system-time variability. It activated
  the predeclared 50-pair confirmation but earned no independent claim.
- In the balanced seed-14014 confirmation, mean user CPU moved 0.384338 s to
  0.379151 s (-1.35%; paired-bootstrap 95% interval -1.87% to +4.61%). Mean
  total CPU moved 0.610030 s to 0.593323 s (-2.74%; interval -1.48% to
  +7.24%), while mean wall moved 0.522907 s to 0.515039 s (-1.50%; interval
  -2.03% to +5.06%). Candidate user/wall p95 regressed 3.55%/6.48%; user and
  total CPU won only 24/50 and 25/50 pairs. Mean RSS improved 0.048%, too small
  to substitute for the failed primary gate.
- Decision card: `keep iff lower95(user_cpu_gain) >= 3% AND
  lower95(total_cpu_gain) >= 3% AND tails/RSS do not regress`. Both confidence
  bounds and both means fail the CPU threshold, and the p95 guard fails.
- Result: REJECTED. No restoration was necessary because this was a build-only
  experiment. Preserved evidence root:
  `/data/tmp/bv-p14-20260824.greenteagc`; confirmation TSV SHA-256
  `05a318b670fad7ab1c8c6d75ae767ec3372aa09d34425003e1764464cec19902`.
  No-Claim: pass 14 adds no measured performance improvement.

### Pass 15 — Correlation-FlatRecordTable — rejected before implementation

- Profile gate: the exact Pass 13 merged baseline contains 2.17 s total samples.
  `newRecordLineSet` holds 0.35 s, but 0.19 s is mandatory record-byte hashing
  and 0.08 s is mandatory newline search. Only 0.08 s / 3.69% remains for all
  map lookup, insertion, entry allocation, growth, and rehash work combined.
  Named map assignment/access account for only 1.84%/0.46%; growth/rehash is
  0.92% and overlaps assignment.
- Amdahl rejection: even deleting that entire 3.69% residual for free leaves
  just 0.69 percentage points above the 3% product gate. A real replacement
  would need probing, allocation, zeroing, and resizing, so it would have to
  remove over 81% of the residual merely to reach the threshold before
  confidence intervals and non-regression guards.
- Prior causal evidence dominates speculation: Pass 11 already removed 96.95%
  of target allocations, reduced B/op 31.53%, and improved the seam 4.40%, yet
  product user CPU improved only 1.22% with a [-0.86%, +3.27%] interval; total
  CPU improved 0.58%, while wall/RSS p95 slightly regressed. Its >=5%
  exclusive-container retry predicate is not true.
- Alien Graveyard check: canonical `/dp/alien_cs_graveyard` section 7.7 calls
  for Swiss-table probe/layout profiling and parity through rehash. Go 1.25's
  current map already exposes Swiss-style `ctrlGroup.matchH2`, `newTable`, and
  `rehash` frames. Replacing it with scalar linear probing is not a new buried
  algorithm, and neither table removes the dominant record-byte hash.
- Rewrite certificate: a future table could preserve current digest-collision
  semantics only by maintaining, after every scanned prefix, `count[h]` equal
  to the number of records with digest `h` and `text[h]` equal to the first such
  line. Zero hashes must remain valid occupied keys; growth must reinsert full
  `uint64` keys; exact-line collision buckets would be behavior-changing.
- Designed negative: force zero and same-bucket distinct hashes through
  wraparound and resize, plus distinct lines with one shared digest and unequal
  multiplicities. Treating hash zero as empty or comparing full text must make
  the oracle fail. No plant was executed because the mutation gate rejected
  implementation first.
- Result: REJECTED / NO IMPLEMENTATION. Retry only when a fresh product profile
  attributes at least 5% exclusive CPU beyond hashing/newline scanning to map
  probes, growth, entry allocation, or GC, and a target prototype removes at
  least 20% of `newRecordLineSet` without a pre-scan or text comparison.
  No-Claim: no files, builds, tests, or measured speedups arose from pass 15.

### Pass 16 — Correlation-StrictDeltaCancellation — rejected by counterexample

- Candidate: exploit the near-append/update shape of successive issue blobs by
  canceling byte-identical whole-line prefixes and suffixes before constructing
  digest multisets, then hash only the changed middle. This is the narrow
  incremental-view/delta-propagation idea from canonical
  `/dp/alien_cs_graveyard` sections 6.1 and 8.15.
- Workload activation: 498 usable old/new snapshot pairs contain 174,428
  record lines and currently hash 384,364,934 bytes. Whole-line prefix/suffix
  cancellation would leave 152,056,507 hash bytes (-60.44%) and remove 61.17%
  of hash invocations. The optimistic Amdahl ceiling is therefore material.
- Hidden cost: certifying equal prefixes/suffixes compares 768,686,298 bytes,
  about twice the current hash input, before hashing the middle. The method is
  not free even on this favorable corpus, and a byte comparison cannot certify
  current digest-bucket semantics.
- Formal rejection: let distinct lines `A != B` share digest `H(A) = H(B)`.
  For old `[A]` and new `[A,B]`, the current multisets have counts one and two
  and both retain first representative `A`, so the synthesized addition is
  `+A`. Canceling the equal prefix leaves old `[]`, new `[B]`, and emits `+B`.
  Thus `cancel(equal prefix); hash(delta)` is not isomorphic to the current
  aggregate-by-digest algorithm. Hashing canceled `A` to detect this possibility
  forfeits the proposed work reduction.
- Designed negative: a forced constant hasher over the `[A]` to `[A,B]` case,
  mirrored suffix cases, unequal multiplicities, CRLF, and middle-record edits
  must distinguish baseline `+A` from the candidate's `+B`. No plant was
  executed because the proof already falsified the rewrite certificate.
- Result: REJECTED / NO IMPLEMENTATION despite strong corpus compression.
  Retry only after an independently justified semantic change to exact line
  identity, or a scheme that proves digest-bucket equivalence without hashing
  canceled records and still retains a >=3% lower-bound product gain.
  No-Claim: pass 16 changed no runtime/test files and measured no candidate
  performance; only the existing evidence ledgers record the disposition.

### Pass 17 — Correlation-TitleValidationSink — rejected before implementation

- Candidate: because correlation never consumes the decoded title value, use a
  zero-sized custom JSON sink that validates `title` but avoids constructing its
  Go string. Omitting title entirely is not exact: the pinned decoder's unknown-
  field skip path does not enforce all string type/escape errors that the
  current typed field rejects.
- Pinned-code proof: goccy/go-json v0.10.6 selects `UnmarshalJSON` before normal
  decoding, calls `skipValue`, allocates `make([]byte, len(src))`, copies the
  complete value, and only then invokes the method. A sink must rescan that copy
  to validate escapes. Normal string decode instead validates in place and
  aliases the already-copied top-level record buffer. The proposed hook adds an
  allocation, copy, scan, and interface call rather than removing work.
- `encoding.TextUnmarshaler` is not an exact escape hatch: this decoder presents
  JSON boolean `true` and JSON string `"true"` as the same `[]byte("true")`, so
  the method cannot retain current type rejection.
- Materiality: the latest 2.17 s merged product profile attributes only 0.08 s /
  3.69% to the entire `parseBeadJSON` frame. The earlier parser-focused corpus
  contains 5,230 title occurrences but only 208 escaped titles. Even a free
  elimination of the whole current frame barely clears a 3% point estimate;
  the actual hook makes the common 5,022 plain titles more expensive.
- Exact semantics matrix includes missing/null/quoted titles, all standard
  escapes, lone surrogate replacement, raw invalid UTF-8/control-byte behavior,
  invalid escapes and non-string types, duplicate recognized fields, malformed
  outer objects, and atomic rejection after partial decode. A value-discarding
  sink still has to preserve every acceptance boundary.
- Designed negative: omitting the typed title or weakening validation must fail
  `title:42`, `title:true`, `title:"\\q"`, `title:"\\u12G4"`, and a valid title
  followed by an invalid duplicate, while retaining accepted null, surrogate,
  and raw-byte cases. No plant was executed because local dependency source and
  the product profile already reject the mechanism.
- Small exact alternative: remove `Title` only from returned `beadSnapshot`
  while retaining `partial.Title string` for decoding. This shrinks a temporary
  snapshot by 16 bytes but retains the same backing record allocation and all
  decoding work; it has no credible >=3% product path and was not changed.
- Result: REJECTED / NO IMPLEMENTATION. Retry only if a fresh product profile
  attributes at least 5% to parsing and an exact whole-record specialized parser
  removes enough of that frame to yield a >=3% lower confidence bound with
  non-increasing B/op and allocations. No-Claim: evidence-only pass.

### Pass 18 recommendation contract — Correlation-TransposedBlobArena

- Activated retry: Pass 6's 64 KiB transport reader reduced `memmove` 45.8%
  and `NewReaderSize` samples 87.5%, but removing the old 10 MiB buffer lowered
  the heap goal and raised GC cycles 28.65%. The current exact profile still
  attributes 17.05% to `blobReader.read`, 13.36% to `readFull`, 6.91% flat to
  `memmove`, and 5.99% flat to `memclr`; the retry predicate is true.
- Transposed allocation: let old transport capacity be `O=10 MiB`, direct-read
  transport `N=64 KiB`, and first valid payload length `S`. Give that payload
  arena `O-N` extra capacity, preserving requested live runway exactly:
  `O + S = N + (S + O - N)`. This transfers heap pacing capacity into the
  already recycled data arena while enabling bufio's large-read bypass.
- Rewrite certificate: payload bytes/length, object request/response order,
  missing-object behavior, errors, live-set aliasing, last-use eviction, and
  one-largest-spare ownership remain unchanged. Missing objects do not consume
  runway. Only capacity placement changes; slices exposed to parsing retain
  length `S`.
- Negative proof: capacity-sum and first-valid-after-missing tests must fail if
  runway is zeroed, consumed by a missing object, duplicated, or exposed in
  payload length. Existing overlapping-history differential and live-buffer
  alias tests remain mandatory.
- Predeclared keep gate: exact normalized output and full correctness gates;
  at least 30% lower merged `memmove`; mean GC cycles no higher than baseline;
  50 balanced low-load pairs with lower-95% user- and total-CPU gains >=3%, CV
  below 10%, and non-regressing system CPU, wall p95/worst, RSS, and read/
  `execve` counts. A positive 12-pair screen only activates confirmation.
- No-Claim: this is a frozen decision card, not an improvement result.

### Pass 18 — Correlation-TransposedBlobArena — rejected, zero runtime change

- Candidate proof: the transport reader moved from 10 MiB to 64 KiB and the
  exact 10,420,224-byte difference became capacity on the first valid payload
  arena. A missing response retained the runway; the first real response
  consumed it; recycled storage retained capacity without aliasing live sets.
  Candidate source SHA-256 values were `d2e7a7f9...` and `cf72fd29...` for
  production/test; exact Go 1.25.5 binary SHA-256 was `5435455a...`.
- Mutation proof: setting only the production constructor's runway to zero made
  the configured-capacity oracle fail with `got 0, want 10420224`; restoring
  the exact candidate made it pass. Focused capacity/lifetime tests, the real
  1,776/1,776 legacy/snapshot differential, full correlation and correlation
  race suites, all-package build/vet, exact formatting/diff checks, and scoped
  UBS with zero critical findings passed on the low-load worker.
- Causal target success: eight merged profiles moved total samples 2.14 s to
  2.03 s (-5.14%), `memmove` 0.26 s to 0.10 s (-61.54%), `readFull` 0.32 s to
  0.25 s (-21.88%), and `blobReader.read` 0.35 s to 0.30 s (-14.29%). Fifteen
  GC-traced pairs moved mean cycles 12.27 to 11.47 (-6.52%; ten wins, five ties,
  zero losses), solving Pass 6's heap-pacing failure. `memclr` samples rose
  from 0.11 s to 0.16 s.
- Product evidence: the 12-pair screen suggested user CPU -5.42%, total CPU
  -2.30%, and wall -1.62%. The first 50-pair confirmation was excluded intact
  when external system work drove total/wall CV above 50%; no trimming or
  mixing occurred. In the fresh seed-18019 50-pair cohort, mean user CPU moved
  0.363070 s to 0.342701 s (-5.61%; paired-bootstrap interval +3.47% to
  +7.66%; 39/50 wins), but baseline user CV was 11.62%.
- Gate failures: total CPU improved 3.14% but its interval was [-1.66%, +7.58%]
  and CVs were 41.44%/31.58%. Wall improved 3.29% with interval [-0.47%,
  +6.83%] and CVs 31.83%/24.13%. Mean RSS regressed 0.061% with a wholly
  negative improvement interval. Two syscall traces per arm increased mean
  reads from 13,804 to 13,909 (+0.76%); writes and eight `execve` calls were
  effectively unchanged.
- Independent-host falsification: the exact two binaries and fixture were moved
  to a second worker whose pre-run load was 0.67. Fifty seed-18021 pairs kept
  all normalized outputs exact, but user CPU improved only 3.78% with interval
  [-0.92%, +7.95%] and CV 14.86%/14.53%. Total CPU regressed 5.08% with interval
  [-16.20%, +4.60%]; system CPU regressed 10.14%; wall regressed 5.14% with
  interval [-14.79%, +3.32%] and p95 regressed 11.29%. RSS was flat. Thus a
  cleaner host did not rescue the combined-resource claim.
- Decision card: user CPU and the target mechanism pass, but lower95(total) is
  below 3%, all primary CV gates fail, and RSS/read-count guards regress. The
  conjunction is false, so retaining the candidate would turn a causal seam win
  into an overstated product claim.
- Disposition: REJECTED by the campaign gate. Restoration did not occur: while
  measurement was still running, a concurrent workflow committed and pushed
  the candidate as `66eafd5a`, rebased `main`, and later committed these ledgers.
  The fresh restoration agent correctly refused to overwrite peer-owned HEAD
  bytes. The operational runtime therefore retains Pass 18, but this campaign
  does not relabel it as an accepted product win. Primary evidence root:
  `/data/tmp/bv-p18-20260824.transposed-blob-arena`; credited confirmation TSV
  SHA-256 `3d028ca8cc06cc4bd8f69f2afea209221925c8077426e3bdd971d5de46fa5f5c`;
  invalid preserved cohort SHA-256 `f2a064d42c4ff5586703524508c430b06032a4b4ce6c861a0ce0ec5b82c1e00f`.
  Independent root `/tmp/bv-p18-independent-20260824` on `vmi1167313`; TSV
  SHA-256 `940f79b1358ae55beabf47b5197a6f48a27be604c187744b0eb4dc801046994e`.
  No-Claim: the retained source has a measured seam/user-CPU effect, but no
  accepted total-CPU, wall, RSS, or portable product improvement.

### Pass 19 — Correlation-CollisionExactSnapshotFrontier — rejected, operational bytes externally retained

- Frontier construction: each live blob now keeps ordered `{start,end,hash}`
  record descriptors. The paired target reuses a digest only after exact
  prefix/suffix byte equality, hashes the unmatched middle, then aggregates
  every target record in target order using target-buffer slices. This preserves
  digest multiplicity and the first representative even for forced collisions;
  it is the collision-exact refinement of Pass 16 rather than line cancellation.
- Proof gates: exact Go 1.25.5 focused tests covered changed middles,
  equal-length unequal lines, prefix/suffix overlap, duplicates, ignored
  non-record lines, CRLF, missing final LF, unindexed/stale references, and
  forced-collision prefix and suffix order. The real history differential was
  1,776/1,776. Ordinary/race correlation, all-package build/vet, formatting,
  diff checks, and exact-file UBS passed; UBS reported zero critical findings
  and only its shadow-workspace `go.mod` warning.
- Mutation proof: an isolated plant skipped the first reused prefix record.
  `A -> [A,B]` under a constant digest failed with `count 1 text B`, versus the
  required `count 2 text A`. Restoring production SHA-256 `2faf6679...` made the
  same test pass. The test file SHA-256 is `314b2ffd...`.
- Direct gate failure: Go 1.25.5 benchmark medians moved only 1,073,589 to
  1,067,814 ns/op (-0.54%), versus the frozen >=25% seam requirement. Hash input
  fell 2,334,720 to 4,560 B/op (-99.80%), but allocations remained 8,244/op and
  bytes/op remained about 1.381 MB. The 768.7 MB exact-comparison burden consumed
  almost all saved hashing work.
- Profile explanation: eight correctly rooted profiles moved total samples only
  2.03 to 2.01 s (-0.99%) and the record builder 0.36 to 0.33 s (-8.33%). AES
  hashing fell 0.16 to 0.06 s, but `memeqbody` appeared at 0.14 s; descriptor
  scanning held 0.11 s cumulative, and `memmove` rose 0.10 to 0.12 s. An initial
  profile set accidentally ran from `/root`, failed to engage repository history,
  and is preserved with zero credit; the credited v2 set asserted the per-commit
  cache artifact after every run from the fixture repo.
- Product screen: all 24 normalized outputs matched SHA-256 `f8f098b0...`.
  Twelve seed-19019 pairs moved mean user CPU 0.357652 to 0.349396 s (-2.31%,
  95% interval -0.66% to +5.37%), total CPU 0.562465 to 0.557015 s (-0.97%,
  interval -4.61% to +6.97%), and wall 0.495000 to 0.487673 s (-1.48%, interval
  -3.57% to +6.71%). System CPU regressed 1.37%; neither CPU point estimate
  reached the 3% confirmation trigger, so no 50-pair cohort was run.
- Disposition: REJECTED by both direct and product gates. During authorship a
  shared workflow committed and pushed production as `a161788e` and tests as
  `2105763f`; restoration would overwrite peer-owned HEAD and therefore did not
  occur. Subsequent passes treat those bytes as operational state without
  promoting them to a campaign performance win. Evidence root:
  `/data/tmp/bv-p19-20260824.collision-exact-frontier`; screen TSV SHA-256
  `2c678a4e4cbb6950c619bbffcf3c6fd086843e4486aec18c1462f5047154b819`.
  No-Claim: exact behavior and hash-work removal are proven; no >=3% user/total,
  latency, RSS, or portable product improvement is accepted.

### Pass 20 — Build-PGOColdTriage — rejected, zero source change

- Training provenance: eight correctly rooted current-runtime profiles merged
  twice under exact Go 1.25.5 to identical SHA-256 `68e226f6...`. They cover the
  complete cold `--robot-triage` command on the pinned fixture, but not warm
  cache, TUI, search, export, other robot routes, non-Linux platforms, or Git
  child code. The merged profile holds 2.01 s of sampled Go CPU across 5.38 s.
- Causal control: `-gcflags=all=-d=pgodebug=3` selected 63 PGO hot-budget
  inlining decisions with the real profile and zero with `-pgo=off`. Thus the
  candidate exercised compiler feedback rather than merely carrying build
  metadata.
- Reproducible builds: two clean-cache `-pgo=off` builds were byte-identical at
  SHA-256 `c4791ef2...`; two clean-cache PGO builds were byte-identical at
  `5e48afc6...`. Both used Go 1.25.5, `-mod=vendor`, `-trimpath`,
  `-buildvcs=false`, CGO, Linux/amd64/v1, and the same source. Build information
  differed only by the candidate's `-pgo=merged-1.pprof` entry (plus ordering);
  size rose 54,048,333 to 54,086,032 bytes (+0.070%).
- Correctness boundary: PGO focused correlation ordinary/race passed, the real
  history differential was 1,776/1,776, and vet passed. The aggregate PGO suite
  is preserved red: a permission-denied loader test cannot fail as root, and one
  UI background-worker test exceeded its 2 s wait under compilation load.
  Unaffected packages and E2E passed; those environmental failures are not
  relabeled as a clean aggregate result.
- Product screen: all 24 normalized outputs matched SHA-256 `f8f098b0...`.
  Twelve seed-20020 pairs moved mean user CPU 0.344163 to 0.335234 s (-2.59%,
  95% interval -3.55% to +8.28%), total CPU 0.555737 to 0.529330 s (-4.75%,
  interval -0.21% to +9.84%), and wall 0.479830 to 0.464381 s (-3.22%, interval
  -2.15% to +8.67%). RSS regressed 0.048%; all intervals cross zero.
- Decision card: confirmation requires both user and total point estimates >=3%
  before spending 50 pairs, followed by lower-95% bounds >=3%, CV <10%, and
  non-regressing tails/RSS. User substitutes as `2.59% >= 3%`, false, so the
  candidate stops at the screen despite the positive total point estimate.
- Result: REJECTED / ZERO SOURCE CHANGE. No `default.pgo`, build flag, release
  asset, or workflow was added. Evidence root:
  `/data/tmp/bv-p20-20260824.pgo-cold-triage`; screen TSV SHA-256
  `3c576c0e8807dee9e00edb81ab1a7c2985a9fdf7c2aa5f99a77e7f3050be46ba`.
  No-Claim: PGO activation is proven; no statistically defensible product CPU,
  latency, RSS, cross-workload, or cross-platform improvement is accepted.

### Pass 21 — Git-DeltaBaseCache32MiB — rejected, zero source change

- Candidate isolation: both arms used the exact same current Go 1.25.5 binary,
  SHA-256 `c4791ef2...`. A pass-private harness applied only to the candidate
  `GIT_CONFIG_COUNT=1`, `GIT_CONFIG_KEY_0=core.deltaBaseCacheLimit`, and
  `GIT_CONFIG_VALUE_0=32m`; `git config --get` returned `32m`. An invalid
  `KEY=VALUE` plant exited one before executing any product run.
- Rationale: the fixture's requested history lives in a 187.79 MiB pack with
  delta depth up to 31. Independent target diagnostics put Git's effective
  96 MiB policy near 111 MiB child RSS, versus about 45 MiB at 32 MiB, with no
  target-time advantage for the larger cache. This activates cache-shaped
  execution from graveyard sections 8.14 and 15.8 without conflating codegen.
- Exact product screen: all 24 normalized outputs matched SHA-256 `f8f098b0...`.
  Twelve seed-21021 pairs moved mean user CPU 0.484090 to 0.497746 s (+2.82%
  regression; 95% improvement interval -10.88% to +4.98%), total CPU 0.855579
  to 0.847143 s (-0.99%; interval -8.71% to +11.65%), and wall 0.731517 to
  0.719146 s (-1.69%; interval -6.02% to +10.41%). CPU/wall CVs ranged from
  21.63% to 46.80%; candidate total/wall p95 and worst regressed.
- Memory mechanism: mean maximum RSS fell 110,216 to 75,202 KiB (-31.77%), with
  12/12 wins and interval [+28.32%, +35.50%]. The cache-size lever is therefore
  real, but the CPU/tail conjunction is false; this campaign does not trade
  unstable runtime for lower peak memory without an explicit memory-only goal.
- Decision card: proceed to 50 pairs and source wiring only if both user and
  total CPU point estimates improve >=3% and tails are non-regressing. User is
  negative and total is 0.99%, so confirmation and implementation are rejected.
- Result: REJECTED / ZERO SOURCE CHANGE. No user Git setting was overridden.
  Evidence root: `/data/tmp/bv-p21-20260824.git-delta-cache`; screen TSV
  SHA-256 `01314f3ae8325fc0a4c99cff382107a394e91e4168d783992c768567098974e0`.
  No-Claim: the 31.77% RSS reduction is measured; no accepted CPU, latency,
  tail, or portable product improvement follows from it.

### Pass 22 — Correlation-TwoRequestWindow — rejected, zero source change

- Profile/materiality anchor: the credited current profile put 0.26 s in
  `blobReader.read`, 0.21 s in `readFull`, and about 0.33 s of record indexing
  after each response. The fixture has 498 unique blobs totaling 384,364,934
  bytes; 496 exceed the worker's measured 65,536-byte stdout pipe. The
  conservative overlap ceiling was about 6.7% command wall, enough for one
  bounded experiment but not a claim.
- Alien construction: graveyard section 8.17's manual fixed-state interleaving
  and the bounded producer/consumer "Bloodstream" pattern became a two-scalar
  cursor plus one pending SHA. Go owns payload N while Git may inflate/write
  response N+1; there is no goroutine, channel, request slice, queue, or second
  Go-owned payload. This directly removes the scheduling mechanism that made
  Pass 13 regress CPU.
- Rewrite/negative certificate: the cursor emits uncached blobs in existing
  `newSHA,oldSHA` first-touch order, skips complete live snapshots, and excludes
  the just-received-but-not-yet-indexed SHA. `lastUse` proves skipped live blobs
  cannot be evicted before their later occurrence. Removing only that scalar
  exclusion changed the oracle from `[a b e f g]` to `[a b b e f g]`; restoring
  production/test SHA-256 `997caf4d...` / `fa707799...` passed. FIFO SHA/type,
  missing, size, payload, trailer, poison, arena, and alias tests passed.
- Exact candidate gates: Go 1.25.5 focused tests, the real 1,776/1,776
  legacy/snapshot differential, full correlation ordinary/race, build, vet,
  formatting, and diff checks passed. Scoped UBS reported zero critical issues;
  its one warning was the known shadow-workspace `go.mod` false boundary.
- Direct seam: eight randomized pairs, three extractions per process, moved mean
  wall 2.689640 to 1.813798 s (-32.56%, 8/8 wins; 95% interval +23.74% to
  +42.05%) and mean total CPU 3.951964 to 2.757087 s (-30.24%). Mean user CPU
  improved 9.52% (7/8); RSS was flat. This clears the diagnostic seam gate but
  runs outside the complete product command.
- Product screen: all 24 normalized outputs matched SHA-256 `f8f098b0...`.
  Twelve seed-22022 pairs moved mean user CPU 0.424639 to 0.416447 s (-1.93%,
  interval -3.87% to +6.88%), total CPU 0.708126 to 0.711543 s (+0.48%
  regression; interval -7.69% to +5.96%), and wall 0.604280 to 0.591029 s
  (-2.19%; interval -3.93% to +7.74%). User/total wins were 7/12 and 6/12;
  all intervals crossed zero.
- Decision/disposition: the frozen screen required at least 3% point gains in
  both user and total CPU before 50 pairs. Both fail, so confirmation and source
  integration stop. The isolated 225-line candidate remains only under `/tmp`
  and the remote evidence root; shared `main` source/test bytes are unchanged.
  Evidence root: `/data/tmp/bv-p22-20260824.two-request-window`; direct TSV
  SHA-256 `b3c28666e787a17e7f1ee3ee4f7f234d1f2095baa639efb1368f3c736f2fd495`,
  product TSV `c62698add43c9b34415e4cc8a2e8c80eab6abbc43c623e248c3aa5824d0625e2`.
  No-Claim: bounded overlap and a direct-seam win are proven; no accepted
  product CPU, latency, RSS, or portable improvement follows.

### Pass 23 — Build-GOAMD64v3 — rejected, zero source change

- Activation/provenance: exact Go 1.25.5 built the same current source twice
  with `GOAMD64=v3`; both binaries were byte-identical at SHA-256 `6ef17de1...`.
  Build metadata recorded Linux/amd64/v3 versus the reproducible v1 control
  `c4791ef2...`, and the EPYC worker exposed AVX2. Correlation ordinary/race
  passed under v3.
- Product screen: all 24 normalized outputs matched `f8f098b0...`. Twelve
  seed-23023 pairs regressed mean user CPU 0.395575 to 0.406274 s (+2.71%;
  improvement interval -8.75% to +2.50%), total CPU 0.637009 to 0.655572 s
  (+2.91%; interval -9.54% to +3.11%), and wall 0.557796 to 0.570407 s
  (+2.26%; interval -8.97% to +3.36%). User, total, and wall p95/worst all
  regressed; RSS was flat.
- Decision: the exact activated codegen loses the 3% screen and the v3 binary
  would also exclude v1/v2 x86-64 hosts. No 50-pair cohort or release-matrix
  change followed. Evidence root `/data/tmp/bv-p23-20260824.goamd64v3`; TSV
  SHA-256 `141e833162c50e99151b5b784b6ee3f7f5c9db0d97490e6e604fe4c16a2670ac`.
  No-Claim: v3 activation/reproducibility is proven; no speedup is accepted.

### Pass 24 — Correlation-FusedSIMDScanFingerprint — rejected before implementation

- Profile bound: the exact current 2.01 s profile assigns 0.11 s to descriptor
  scanning, including 0.05 s assembly `indexbytebody`, and only 0.04 s
  cumulative to `hashRecordLine` / 0.06 s flat AES hashing. Exact fusion retains
  delimiter discovery, descriptor emission, and the current digest. Even a
  physically free overlap erases at most `min(0.05,0.04)=0.04 s`, 1.99% of the
  command, below the 3% point gate before call/dispatch overhead.
- Causal negative: disassembly of the exact v1 binary proves `indexbytebody`
  already dispatches to 16-byte SSE and 32-byte AVX paths, while `aeshashbody`
  uses AESENC assembly. Replacing private-seed maphash with a different fused
  hash changes collision buckets and first-representative behavior; keeping it
  requires the same second pass. Pass 19's stronger empirical control removed
  99.80% of hashed bytes yet improved the seam only 0.54% and product user/total
  only 2.31%/0.97%.
- Decision: the Amdahl and semantics conjunction is false, so no assembly,
  unsafe code, architecture files, tests, or product cohort were created.
  Evidence root `/data/tmp/bv-p24-20260824.fused-scan-hash`; exact disassembly
  hashes are preserved there. Retry only if scanning plus hashing exceeds 10%
  exclusive product CPU or the digest contract intentionally changes.
  No-Claim: existing SIMD activation and a sub-gate ceiling are proven.

### Pass 25 — Git-PackTopologyOrder — rejected, zero source change

- Corpus proof: the exact 498 first-touch OIDs represent 384,364,934 logical
  bytes but only 1,725,070 packed bytes in one 187.79 MiB pack. Relevant delta
  depths span zero to 49 (mean 17.22), so topology is real rather than guessed.
- Direct falsification: eight warm `git cat-file --batch` runs per order moved
  the same OID set. Current newest-first averaged 0.10000 s wall, 0.06125 user,
  and 0.03125 system. Reversing it regressed wall to 0.14625 s and system to
  0.07500 s. Sorting by pack offset averaged 0.10125 s wall and increased RSS
  about 1.64%; it produced no time win even before accounting for Go.
- Rewrite/lifetime boundary: decoupling response order from commit first-touch
  would require keyed reassociation and retaining some or all payloads; the
  full pack-offset form sacrifices the current bounded live-window guarantee
  for up to 384 MB of logical blobs. A zero producer win cannot pay that cost.
- Decision: no source, pack mutation/repack, or product cohort followed.
  Evidence root `/data/tmp/bv-p25-20260824.pack-order`; newest/reverse/pack-order
  OID and timing hashes are preserved there. Retry only after a different pack
  topology makes pack order at least 10% faster without widening live memory.
  No-Claim: current request order wins this fixture comparison; no new speedup.

### Pass 26 — Correlation-CatFileRequestTrains — rejected, zero source change

- Construction: a two-scalar first-touch cursor filled a fixed seven-SHA train;
  seven SHA-256 requests plus newlines are 455 bytes, below POSIX's 512-byte
  minimum atomic pipe write. One flush replaced seven request/response parent
  handshakes while response consumption, one-payload ownership, last-use
  eviction, cache holes, and `newSHA,oldSHA` order remained unchanged. No
  goroutine, channel, dynamic queue, or complete request plan was introduced.
- Negative/positive proof: mutating only train width seven to one changed the
  oracle's first submission from `[a b c d e f g]` to `[a]`; exact restoration
  at source/test SHA-256 `dc52a7d7...` / `8927c00f...` passed. Focused FIFO,
  missing, arena, alias, overlapping-history, and frontier tests passed under
  Go 1.25.5.
- Direct gate: eight randomized three-extraction pairs moved wall 1.029254 to
  0.950443 s (-7.66%, 6/8; interval -1.25% to +17.80%), but user CPU was flat
  at +0.01% regression and total CPU improved only 0.07%; RSS regressed 0.07%.
  The point wall gain missed the predeclared 10% seam gate and every primary
  interval crossed zero. A syscall trace confirmed a changed execution shape,
  but traced timing is diagnostic only.
- Decision: no product screen, 50-pair cohort, or source integration followed.
  Evidence root `/data/tmp/bv-p26-20260824.catfile-request-trains`; direct TSV
  SHA-256 `4d3c879905f8ee7935b3b1319461ff351f20e96d0c5d2223a70f8edd4d7af9df`.
  No-Claim: bounded train activation is proven; no product or CPU win is.

### Pass 27 — Correlation-DirectEventFusionV2 — rejected, zero source change

- Reopen predicate: the current post-P18/P19 profile still attributes 0.20 s to
  `synthesizeRecordDiff` and 0.15 s to `parseDiff`, 17.4% cumulative command
  CPU. The candidate fed changed digest representatives directly into the same
  sorted event finalizer, with conservative fallback for Scanner-sized records
  and ambiguous same-side IDs; exact collision/multiplicity semantics remained
  those of the current record sets.
- Semantic proof: changing only the new-side multiplicity guard from `<=` to
  `<` made identical two-copy sets emit one event; restoration at source hashes
  `81b0617f...` / `8dbd3844...` / test `78cb60e7...` passed. Forced collisions,
  filtering, create/status cases, same-ID fallback, focused lifetime tests, and
  the real 1,776/1,776 legacy/snapshot differential passed. Exact Go 1.25.5
  product binaries were `c4791ef2...` baseline and `4a746ea9...` candidate.
- Direct evidence: eight randomized pairs improved mean user CPU 6.12% (8/8),
  total CPU 5.65%, and wall 4.65%; benchmark bytes/op fell 96,259,608 to
  59,478,808 (-38.21%) and allocations 116,703 to 115,506 (-1.03%). This
  cleared the direct and 12-pair product triggers.
- Confirmation: all 100 seed-27028 outputs normalized to `f8f098b0...`. Mean
  user CPU moved 0.306648 to 0.290279 s (-5.34%, interval +2.78% to +7.84%),
  total CPU 0.478452 to 0.458849 s (-4.10%, interval +1.51% to +6.68%), and wall
  0.415979 to 0.403622 s (-2.97%, interval +0.40% to +5.66%). User/total/wall
  p95 and worst all improved, candidate CVs were below 10%, and RSS regressed
  0.035% with an interval crossing zero. Baseline total/wall CVs were
  12.36%/10.95%.
- Decision: the frozen keep gate requires lower-95% user AND total gains >=3%
  and sub-10% cohort CV. Substitution gives `2.78 >= 3` and `1.51 >= 3`, both
  false, plus two baseline CV failures. The promising point/tail result cannot
  relax the rule after observation; source remains isolated. Evidence root
  `/data/tmp/bv-p27-20260824.direct-event-fusion-v2`; direct/confirmation TSV
  SHA-256 `0d8dfc655ea024e8213430083ef7e622b91f3c92c75e9ef3d02edecb05dd0ed0` /
  `9690c88a10444a34e3c30d4bf1faff78336589c215dadb27cee3a765a0087524`.
  No-Claim: allocation and point CPU/tail gains are real; no accepted runtime
  improvement follows under the campaign gate.

### Pass 28 — Correlation-SingleIDDirectFusion — rejected, zero source change

- Profile/retry anchor: Pass 27 left a promising but unaccepted direct-event
  candidate and named a zero-map single-ID specialization as its next causal
  retry. The exact current 2.01 s profile still assigns 0.20 s to synthesized
  diffs and 0.15 s to parsing them; the candidate replaced three maps plus ID
  sorting only when all changed representatives belong to one bead, and fell
  back to Pass 27's exact map path for ambiguous or multi-ID commits.
- Semantic certificate: a shared scalar finalizer preserved create, status,
  filter, multiplicity, collision, and event-order behavior. Inverting only the
  new-side ID guard made the status-transition test fail with `direct path
  unexpectedly requested fallback`; exact restoration to snapshot-source
  SHA-256 `d0445c81...` passed all direct-path tests. Focused frontier/lifetime
  tests and the real-history differential passed at 1,776/1,776 events under
  exact Go 1.25.5.
- Direct screen: eight randomized three-extraction pairs compared current
  production (`2390bfd6...` test binary) with the combined Pass-27/28 candidate
  (`dd659464...`). Bytes/op fell from 94,987,705 to 87,555,412 (-7.82%) and
  allocations from 116,606 to 116,524 (-0.07%), but mean user CPU regressed
  1.25%, system CPU regressed 1.25%, wall regressed 1.31%, and RSS regressed
  0.088%. User wins were 5/8 and wall wins only 3/8.
- Decision: the specialization makes multi-ID commits fall back and therefore
  recovers much less allocation than Pass 27's general direct path while adding
  scalar branching. It fails the direct CPU gate, so no product cohort or
  shared-source integration follows. The first harness launch used a nonexistent
  fixture path and is preserved but receives zero credit; the corrected run used
  `/data/tmp/bv-pass13.5WGZWS`. Evidence root:
  `/data/tmp/bv-p28-20260824.single-id-fusion`; credited TSV SHA-256
  `634746e0300c99ab2e49b029d52aefc8ccdf14a03c9acc4d3c22d19729b954d1`.
  No-Claim: the scalar path and allocation reduction are proven; no runtime,
  latency, RSS, or product improvement is accepted.
